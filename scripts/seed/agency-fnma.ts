/**
 * scripts/seed/agency-fnma.ts — FNMA agency_rule_version + agency_rule seed.
 *
 * Phase 3 Wave 0 stub created by Plan 03-01 (Task 08 / B1a fix from
 * REVIEWS.md iter-2). Plan 03-02 REPLACES the body of `seedFnma()` with the
 * real FNMA Selling Guide derog-seasoning + reestablishment + extenuating-
 * circumstances seed. The function exists from Wave 0 onward so Wave 0
 * `pnpm db:seed` produces zero FNMA rows but the aggregator import resolves.
 */

export async function seedFnma(): Promise<void> {
  // STUB: Plan 03-02 swaps this with the real FNMA seed body.
  // Stub is intentionally a no-op so Wave 0 db:seed produces zero FNMA rows.
}
