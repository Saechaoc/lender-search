/**
 * scripts/seed/agency-fhlmc.ts — FHLMC agency_rule_version + agency_rule seed.
 *
 * Phase 3 Wave 0 stub created by Plan 03-01 (Task 08 / B1a fix from
 * REVIEWS.md iter-2). Plan 03-03 REPLACES the body of `seedFhlmc()` with
 * the real FHLMC Single-Family Seller/Servicer Guide derog-seasoning seed.
 */

export async function seedFhlmc(): Promise<void> {
  // STUB: Plan 03-03 swaps this with the real FHLMC seed body.
}
