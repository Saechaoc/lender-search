/**
 * scripts/seed/agency-fha.ts — FHA agency_rule_version + agency_rule seed.
 *
 * Phase 3 Wave 0 stub created by Plan 03-01 (Task 08 / B1a fix from
 * REVIEWS.md iter-2). Plan 03-04 REPLACES the body of `seedFha()` with the
 * real HUD Handbook 4000.1 derog-seasoning + Back-to-Work (DEPRECATED state
 * with sunset_date 2016-09-30) + MULTIPLE_BK + extenuating-circumstances seed.
 */

export async function seedFha(): Promise<void> {
  // STUB: Plan 03-04 swaps this with the real FHA seed body.
}
