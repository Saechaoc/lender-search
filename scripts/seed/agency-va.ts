/**
 * scripts/seed/agency-va.ts — VA agency_rule_version + agency_rule seed.
 *
 * Phase 3 Wave 0 stub created by Plan 03-01 (Task 08 / B1a fix from
 * REVIEWS.md iter-2). Plan 03-05 REPLACES the body of `seedVa()` with the
 * real VA Pamphlet 26-7 derog-seasoning + MULTIPLE_BK + 4 sentinel rows
 * (FORECLOSURE/DIL/SS/CHARGE_OFF with not_applicable=true) seed.
 */

export async function seedVa(): Promise<void> {
  // STUB: Plan 03-05 swaps this with the real VA seed body.
}
