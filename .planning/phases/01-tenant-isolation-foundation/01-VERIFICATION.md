---
phase: 01-tenant-isolation-foundation
verified: 2026-04-29T00:00:00Z
status: human_needed
score: 5/5 success criteria verified locally; 8/8 requirement IDs satisfied at Phase 1 contract layer
overrides_applied: 0
human_verification:
  - test: "Push the branch to GitHub and confirm the CI workflow runs green"
    expected: "All 11 steps in .github/workflows/ci.yml pass: checkout → pnpm + Node 20 → install → provision app_user → typecheck → lint → lint:fixture → migrate + GRANT → FORCE RLS gate → pnpm test:rls"
    why_human: "GitHub Actions infrastructure cannot be invoked from this verification environment. Local smoke ran 27/27 tests green in 657ms; the CI workflow is a verbatim replication so green-on-CI is highly likely, but only a real push proves the merge gate operates correctly."
  - test: "Demonstrate CI catches a deliberate regression"
    expected: "Temporarily change `expect(rows).toHaveLength(0)` to `1` in tests/rls/cross-tenant-select.test.ts, push, confirm CI turns red, revert, confirm CI turns green"
    why_human: "The intentional-break-and-revert flow requires pushing real commits to a remote, watching CI react in real time, and judging that the failure surfaced is the expected one. Plan 08 SUMMARY documents this as the explicit Plan 08 Task 4 follow-up after the auto-approved checkpoint."
  - test: "Configure branch protection rules on `main` to require the CI / verify check before merge"
    expected: "Repository settings → Branches → Branch protection rules → main → 'Require status checks to pass before merging' includes 'CI / verify'"
    why_human: "Branch protection is a GitHub repo-admin operation outside the codebase. Without it, the CI workflow runs but does not actually block merge — a maintainer could merge a red PR. Plan 08 SUMMARY flags this as a separate manual user step."
---

# Phase 1: Tenant Isolation Foundation — Verification Report

**Phase Goal (from ROADMAP.md):** Postgres database stands up with `FORCE ROW LEVEL SECURITY` on every tenant-scoped table; CI pen-test suite blocks merge on any cross-tenant query that succeeds. This is the antitrust insurance policy — every later phase depends on it being in place before any tenant data exists.

**Verified:** 2026-04-29
**Status:** `human_needed` — all 5 ROADMAP success criteria pass against the live codebase (typecheck=0, lint=0, lint:fixture=0, test:rls=27/27 pass in 657ms, live psql introspection confirms `relrowsecurity=t` AND `relforcerowsecurity=t` for both tables, both policies present, indexes present, role privileges correct). Three items still need human action: actually pushing the branch and watching the GitHub Actions workflow run; demonstrating CI catches a deliberate regression; configuring branch protection on `main` to require the CI check.

**Re-verification:** No — initial verification.

---

## Goal Achievement

### Observable Truths (5 ROADMAP Success Criteria)

| #   | Truth                                                                                                                                                                                                                                                                                          | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | A query authenticated as Tenant A returns zero rows when targeting Tenant B's data on every tenant-scoped table (verified by `tests/rls/` CI pen-test suite blocking merge on success)                                                                                                         | ✓ VERIFIED | `pnpm test:rls` passed 27/27 in 657ms against live Postgres 16. `tests/rls/cross-tenant-select.test.ts` (4 it blocks) and `tests/rls/cross-tenant-write.test.ts` (4 it blocks) directly assert this; pg_policy confirms `tenant_self_filter` and `canary_tenant_isolation` are active with USING + WITH CHECK clauses on the GUC.                                                       |
| 2   | Postgres 16+ via Supabase is provisioned with Drizzle ORM 0.45 connected with `prepare: false`; migrations apply cleanly from a fresh database                                                                                                                                                 | ✓ VERIFIED (with deferral) | Phase 1 contract met: `docker-compose.yml` uses `postgres:16-alpine`; `package.json` pins `drizzle-orm@0.45.2`; `lib/db/client.ts:29` contains `prepare: false`. `pnpm drizzle-kit migrate` applies 0000_initial.sql + 0001_force_rls.sql cleanly (verified live). The "via Supabase" cloud-project portion is deferred to Phase 6 per orchestrator's explicit acceptance note (D-07 / D-08). |
| 3   | JWT-tampering tests, service-role boundary tests, cross-tenant write attempts, and cross-tenant read attempts all fail closed                                                                                                                                                                  | ✓ VERIFIED | All four test files exist and pass live: `tests/rls/jwt-tampering.test.ts` (3 it blocks asserting forged JWT → 0 rows), `tests/rls/service-role-boundary.test.ts` (3 it blocks asserting SUPABASE_SERVICE_ROLE_KEY undefined + DATABASE_URL points at app_user), `tests/rls/cross-tenant-write.test.ts` (4 it blocks asserting WITH CHECK rejection + 0-rows-affected), `tests/rls/cross-tenant-select.test.ts` (4 it blocks). All green in the 27/27 live run. |
| 4   | `tenant_id` is indexed on every tenant-scoped table; RLS policies use `current_setting('app.tenant_id')::uuid` from JWT app_metadata                                                                                                                                                           | ✓ VERIFIED (with seam) | Live `pg_indexes` shows `rls_canary_tenant_idx`. `tests/rls/index-scan.test.ts` (2 it blocks) confirms EXPLAIN uses Index Scan with `enable_seqscan=off` AND that `rls_canary_tenant_idx` appears in plan output. Policy SQL in `db/migrations/0000_initial.sql` and live `pg_policy` use `current_setting('app.tenant_id', true)::uuid`. The "JWT app_metadata" seam: Phase 1 uses jose-minted fixture JWTs (Plan 06 `tests/rls/fixtures/jwt.ts`); Phase 6 swaps to real Supabase JWKS verification (`setTenantContext` primitive contract documented for that). |
| 5   | No environment-variable plaintext secrets land in source; t3-env validates all required keys at boot                                                                                                                                                                                            | ✓ VERIFIED (with deferral) | `lib/env.ts` uses `@t3-oss/env-core` + Zod 4 with `DATABASE_URL` (URL + Postgres-prefix `refine`), `RLS_TEST_JWT_SECRET` (min 16 chars optional), `SUPABASE_SERVICE_ROLE_KEY` (optional@P1), `emptyStringAsUndefined: true`. `tests/rls/env-boot.test.ts` (8 it blocks) proves boot-fail-closed semantics. `.env.local` is gitignored; `.env.local.example` is committed. ESLint rule (`eslint.config.mjs`) blocks `process.env` reads from `app/**`/`pages/**`/`src/**`; `pnpm lint:fixture` proves the rule fires (2 errors on the fixture). Vercel encrypted env wiring is deferred to Phase 6 (no Vercel project exists at Phase 1). |

**Score:** 5/5 truths verified locally.

---

### Required Artifacts (3-level + Level 4 verification)

All Phase 1 artifacts pass Levels 1 (existence), 2 (substantive — non-stub), 3 (wired — imports/usage traced), and 4 (data flow — pen tests prove behavior end-to-end).

| Artifact                                  | Expected                                                                                                          | Status     | Details                                                                                                                                                                                                                                                                                              |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `lib/env.ts`                              | t3-env Zod schema with DATABASE_URL required URL                                                                  | ✓ VERIFIED | 51 lines; `createEnv` from `@t3-oss/env-core`; `DATABASE_URL: z.string().url().refine(...)`. Wired by `drizzle.config.ts` (DATABASE_URL load), `lib/db/client.ts` (`env.DATABASE_URL`), pen tests via dotenv. 8/8 env-boot tests pass live.                                                              |
| `lib/db/client.ts`                        | Drizzle postgres-js client with `prepare: false`                                                                  | ✓ VERIFIED | 42 lines; imports `drizzle` from `drizzle-orm/postgres-js`, `postgres` from `postgres`, `env` from `../env.js`; `prepare: false` at line 29. Phase 1 has no app code consuming this yet (Phase 6's job); the contract is in place.                                                                  |
| `lib/tenant/context.ts`                   | setTenantContext using `set_config('app.tenant_id', _, true)`                                                     | ✓ VERIFIED | 71 lines; canonical primitive at line 61: `await tx.execute(sql\`SELECT set_config('app.tenant_id', ${tenantId}, true)\`)`. Single source of truth verified — see "Single source of truth" section. JSDoc documents Phase 6 `withTenantContext` consumer contract.                                  |
| `db/schema/tenant.ts`                     | tenant table + tenant_kind enum + self-filtering policy                                                           | ✓ VERIFIED | 60 lines; pgEnum `tenant_kind` with all four locked values; `pgTable('tenant', ...)` with self-filtering `pgPolicy('tenant_self_filter', ...)` USING + WITH CHECK on `id = current_setting('app.tenant_id', true)::uuid`.                                                                            |
| `db/schema/canary.ts`                     | _rls_canary table with FK to tenant, indexed tenant_id, RLS policy on tenant_id                                   | ✓ VERIFIED | 53 lines; FK `references(() => tenant.id)`; `index('rls_canary_tenant_idx').on(t.tenantId)`; `pgPolicy('canary_tenant_isolation', ...)` USING + WITH CHECK on `tenant_id = current_setting('app.tenant_id', true)::uuid`. Live pg_indexes confirms index exists.                                       |
| `db/schema/index.ts`                      | Schema barrel re-exports                                                                                          | ✓ VERIFIED | 5 lines; `export * from './tenant.js'; export * from './canary.js';` (NodeNext .js extensions). drizzle.config.ts schema field points at this.                                                                                                                                                       |
| `db/migrations/0000_initial.sql`          | Generated migration: CREATE TYPE/TABLE×2/INDEX/ENABLE RLS×2/CREATE POLICY×2                                       | ✓ VERIFIED | 23 lines (incl. statement-breakpoints). Contains: `CREATE TYPE "public"."tenant_kind" AS ENUM(...)`, `CREATE TABLE "tenant"`, `CREATE TABLE "_rls_canary"`, `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` (×2), `CREATE INDEX "rls_canary_tenant_idx"`, `CREATE POLICY "tenant_self_filter"`, `CREATE POLICY "canary_tenant_isolation"`. NO `FORCE ROW LEVEL SECURITY` (Plan 05 owns that — verified absent via `! grep`). |
| `db/migrations/0001_force_rls.sql`        | FORCE ROW LEVEL SECURITY for both tables                                                                          | ✓ VERIFIED | 21 lines (16 lines of explanatory comments + 2 ALTER + blank). Lines 19-20: `ALTER TABLE "tenant" FORCE ROW LEVEL SECURITY;` and `ALTER TABLE "_rls_canary" FORCE ROW LEVEL SECURITY;`. Live pg_class confirms applied (`relforcerowsecurity = t` for both).                                          |
| `db/migrations/meta/_journal.json`        | Journal lists both 0000 and 0001                                                                                  | ✓ VERIFIED | Both entries present with idx 0 and idx 1; tags `0000_initial` and `0001_force_rls`; version 7.                                                                                                                                                                                                       |
| `tests/rls/global-setup.ts`               | Vitest globalSetup applies migrations + grants                                                                    | ✓ VERIFIED | 91 lines; uses `execFileSync` (NOT `execSync`); TRUNCATE (gated on `to_regclass`) → `pnpm drizzle-kit migrate` → GRANT DML to app_user. Wired into `vitest.config.ts` `globalSetup` array.                                                                                                            |
| `tests/rls/setup.ts`                      | Per-file setup: pg.Pool, BYPASSRLS sanity, FORCE RLS sanity, policy presence sanity                              | ✓ VERIFIED | 98 lines; opens `globalThis.__pgPool` as app_user; beforeAll asserts `pg_roles.rolbypassrls = false` AND `pg_class.relforcerowsecurity = true` for both tables AND both policies present. Wired into `vitest.config.ts` `setupFiles` array.                                                          |
| `tests/rls/fixtures/jwt.ts`               | mintJWT, forgeJWT, extractTenantIdFromJWT (jose@6 HS256)                                                          | ✓ VERIFIED | 84 lines; `import { SignJWT } from 'jose';` + `getSecret()` enforces min-16 on RLS_TEST_JWT_SECRET. Imported by `tests/rls/jwt-tampering.test.ts`.                                                                                                                                                     |
| `tests/rls/fixtures/tenants.ts`           | seedTwoTenants implementing the bootstrap pattern                                                                 | ✓ VERIFIED | 114 lines; bootstrap pattern: `gen_random_uuid()::text` → `set_config('app.tenant_id', $1, true)` → `INSERT INTO tenant (id, ...)` → `INSERT INTO _rls_canary (tenant_id, ...) RETURNING id::text`. Two separate BEGIN/COMMIT transactions per call. Imported by 6 of 6 cross-tenant test files.        |
| `tests/rls/fixtures/connection.ts`        | connectAsTenant + connectAsAnonymous transactional helpers                                                        | ✓ VERIFIED | ~80 lines; both helpers use `BEGIN → set_config (or skip for anon) → fn → ROLLBACK` pattern. `connectAsAnonymous` uses fresh `pg.Client` (Plan 07 Postgres-16 placeholder-GUC fix). is_local=true (Pitfall 3 mitigated).                                                                              |
| `tests/rls/cross-tenant-select.test.ts`   | TNT-02 read coverage                                                                                              | ✓ VERIFIED | 4 it blocks; all green in live run. Asserts A's GUC + B's id → 0 rows; unset GUC → 0 rows; sanity reads; tenant self-filtering policy.                                                                                                                                                                |
| `tests/rls/cross-tenant-write.test.ts`    | TNT-02 write coverage                                                                                             | ✓ VERIFIED | 4 it blocks; all green. INSERT mismatched tenant_id throws WITH CHECK; UPDATE on B-row from A → 0 rows; DELETE on B-row from A → 0 rows; in-tenant INSERT sanity.                                                                                                                                     |
| `tests/rls/jwt-tampering.test.ts`         | T-01-03 spoofing structural defense                                                                               | ✓ VERIFIED | 3 it blocks; all green. forge + extract round-trip; forged JWT to non-existent tenant → 0 rows; tampered claim cannot read foreign rows.                                                                                                                                                              |
| `tests/rls/service-role-boundary.test.ts` | T-01-04 env contract                                                                                              | ✓ VERIFIED | 3 it blocks; all green. SUPABASE_SERVICE_ROLE_KEY undefined; DATABASE_URL contains 'app_user' and not 'postgres:postgres@'; zero env keys with 'SERVICE_ROLE' substring.                                                                                                                              |
| `tests/rls/guc-reset.test.ts`             | T-01-05 GUC leak prevention                                                                                       | ✓ VERIFIED | 3 it blocks; all green. Post-ROLLBACK value did not survive (assertion shape corrected for Postgres 16 placeholder-GUC semantics; both NULL and '' are fail-closed); two consecutive connectAsTenant calls do not leak; set_config sanity.                                                            |
| `tests/rls/index-scan.test.ts`            | TNT-03 EXPLAIN-uses-Index-Scan                                                                                    | ✓ VERIFIED | 2 it blocks; all green. `rls_canary_tenant_idx` in pg_indexes; EXPLAIN with `SET LOCAL enable_seqscan = off` produces plan containing both `/Index (Scan|Only Scan)|Bitmap Index Scan/` and the literal `rls_canary_tenant_idx`.                                                                       |
| `tests/rls/env-boot.test.ts`              | CFG-05 boot-fail-closed contract                                                                                  | ✓ VERIFIED | 8 it blocks; all green inside the full vitest config. Empty / malformed / wrong-scheme DATABASE_URL throws; valid Postgres URL succeeds; short JWT secret throws; missing optional service-role-key OK; NODE_ENV default.                                                                            |
| `eslint.config.mjs`                       | Flat config blocking SUPABASE_SERVICE_ROLE_KEY + process.env from app paths; service-role* / admin-db* imports     | ✓ VERIFIED | 87 lines; flat config (ESM); `tseslint.configs.recommended` base; `no-restricted-properties` blocks `process.env` for `app/**`, `pages/**`, `src/**`; `no-restricted-imports` blocks `**/service-role*` and `**/admin-db*` patterns. Legacy CRA prototype files in global ignores list.                |
| `app/.eslint-fixture.ts`                  | Decoy file containing forbidden patterns; lint must reject                                                       | ✓ VERIFIED | 30 lines with 2 violations (DATABASE_URL and SUPABASE_SERVICE_ROLE_KEY reads via process.env) + comment-pinned forbidden import patterns. `pnpm lint:fixture` reports 2 `no-restricted-properties` errors and exits 0 (inverted exit code — proves rule fires).                                       |
| `.github/workflows/ci.yml`                | CI: postgres:16 service container, pnpm test:rls                                                                  | ✓ VERIFIED | 109 lines; valid YAML (parses); triggers on PR + push to main; concurrency cancel-in-progress; postgres:16-alpine service container with healthcheck; 11 ordered steps including provision app_user → typecheck → lint → lint:fixture → migrate + GRANT → FORCE RLS gate → `pnpm test:rls`.        |
| `docker-compose.yml`                      | postgres:16-alpine + healthcheck + init-db.sh bind mount                                                          | ✓ VERIFIED | 24 lines; `image: postgres:16-alpine`; named volume `lender-search-pg-data`; init script bind-mount `:ro`; pg_isready healthcheck. Container observed live as `Up (healthy)`.                                                                                                                          |
| `scripts/init-db.sh`                      | Provisions app_user with NOBYPASSRLS NOSUPERUSER                                                                  | ✓ VERIFIED | Executable bit set. Contains `CREATE ROLE app_user LOGIN PASSWORD 'app_user_password' NOBYPASSRLS NOSUPERUSER`. Live `pg_roles` confirms `rolbypassrls=f, rolsuper=f`.                                                                                                                                |
| `package.json`                            | TS project, pinned deps, pnpm scripts                                                                             | ✓ VERIFIED | Single-package; `packageManager: "pnpm@9.15.0"`; deps pinned EXACTLY (no ^/~) at the canonical Phase 1 versions: drizzle-orm@0.45.2, drizzle-kit@0.31.10, postgres@3.4.9, pg@8.20.0, vitest@4.1.5, jose@6.2.3, zod@4.4.1, @t3-oss/env-core@0.13.11, typescript@5.7.3, eslint@10.2.1, typescript-eslint@8.59.1. NO react-scripts. |
| `pnpm-workspace.yaml`                     | Repo root pnpm config                                                                                             | ✓ VERIFIED | Single-package workspace.                                                                                                                                                                                                                                                                              |
| `tsconfig.json`                           | TypeScript 5.7 strict + NodeNext + src/ excluded                                                                  | ✓ VERIFIED | strict, ES2022 target, NodeNext module + moduleResolution, noUncheckedIndexedAccess + noImplicitOverride; include lib/db/tests + drizzle.config.ts + vitest.config.ts; exclude `src` (legacy CRA isolated).                                                                                              |
| `vitest.config.ts`                        | pool: 'forks', globalSetup, setupFiles                                                                            | ✓ VERIFIED | `pool: 'forks'`, `isolate: true`, `globalSetup: ['./tests/rls/global-setup.ts']`, `setupFiles: ['./tests/rls/setup.ts']`, `include: ['tests/rls/**/*.test.ts']`, sequence concurrent: false, 10s timeouts.                                                                                            |
| `vitest.env-boot.config.ts`               | Standalone runner (Wave 1 timing escape hatch)                                                                    | ✓ VERIFIED | Created Plan 02 to bypass missing globalSetup before Plan 06 landed. Now redundant (env-boot.test.ts runs in main suite) but kept as fast-path regression smoke per Plan 02 SUMMARY.                                                                                                                  |
| `drizzle.config.ts`                       | drizzle-kit config + dual env loading                                                                             | ✓ VERIFIED | Loads .env.local first then .env (Plan 05 deviation fix). `DATABASE_MIGRATION_URL` preferred over `DATABASE_URL` for migrations. `dialect: 'postgresql'`, schema './db/schema/index.ts', out './db/migrations'.                                                                                          |
| `.env.local.example`                      | Documents env contract                                                                                            | ✓ VERIFIED | Documents `DATABASE_URL` (app_user), `DATABASE_MIGRATION_URL` (postgres superuser), `RLS_TEST_JWT_SECRET`, commented `SUPABASE_SERVICE_ROLE_KEY` placeholder. `.env.local` itself gitignored.                                                                                                          |

---

### Key Link Verification

| From                                              | To                                                | Via                                                                        | Status   | Details                                                                                                                                                                                       |
| ------------------------------------------------- | ------------------------------------------------- | -------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `lib/db/client.ts`                                | `lib/env.ts`                                      | `import { env } from '../env.js'`                                          | ✓ WIRED  | Line 23.                                                                                                                                                                                      |
| `lib/tenant/context.ts`                           | Postgres GUC `app.tenant_id`                      | `set_config('app.tenant_id', ${tenantId}, true)`                           | ✓ WIRED  | Line 61. Third arg `true` (is_local) confirmed.                                                                                                                                                |
| `db/schema/tenant.ts` + `db/schema/canary.ts`     | GUC `app.tenant_id`                              | `current_setting('app.tenant_id', true)::uuid` in pgPolicy USING + WITH CHECK | ✓ WIRED  | Both tables: line 52-53 (tenant) + line 45-46 (canary). Live pg_policy confirms.                                                                                                              |
| `db/schema/canary.ts`                             | `db/schema/tenant.ts`                             | `tenantId.references(() => tenant.id)`                                     | ✓ WIRED  | Line 31. Live pg_constraint confirms FK `_rls_canary_tenant_id_tenant_id_fk`.                                                                                                                  |
| `drizzle.config.ts schema field`                  | `db/schema/index.ts`                              | `schema: './db/schema/index.ts'`                                           | ✓ WIRED  | Verified.                                                                                                                                                                                      |
| `vitest.config.ts globalSetup`                    | `tests/rls/global-setup.ts`                       | `globalSetup: ['./tests/rls/global-setup.ts']`                             | ✓ WIRED  | Confirmed by live `pnpm test:rls` output: `[✓] migrations applied successfully!` from globalSetup before tests run.                                                                            |
| `vitest.config.ts setupFiles`                     | `tests/rls/setup.ts`                              | `setupFiles: ['./tests/rls/setup.ts']`                                     | ✓ WIRED  | Confirmed by live test run not aborting (BYPASSRLS + FORCE + policy sanity checks all passed in beforeAll).                                                                                    |
| `tests/rls/setup.ts`                              | Postgres `pg_class.relforcerowsecurity`           | `beforeAll` introspection asserts `relforcerowsecurity=true`               | ✓ WIRED  | Lines 50-65. Live pg_class confirms `t` for both tables.                                                                                                                                       |
| `tests/rls/setup.ts`                              | `pg_roles.rolbypassrls`                          | `beforeAll` introspection asserts current_user is NOT BYPASSRLS            | ✓ WIRED  | Lines 41-50. Live pg_roles confirms `app_user.rolbypassrls=f`.                                                                                                                                  |
| `db/migrations/0001_force_rls.sql`                | live pg_class                                     | `pnpm drizzle-kit migrate` applies the ALTER                                | ✓ WIRED  | Live `pg_class.relforcerowsecurity = t` for both `tenant` and `_rls_canary`. _journal.json shows both migrations applied.                                                                      |
| `tests/rls/*.test.ts`                             | Plan 06 fixtures                                  | `import { ... } from './fixtures/{connection,tenants,jwt}.js'`             | ✓ WIRED  | Verified by 27/27 tests passing — every test file imports fixtures and uses them.                                                                                                              |
| `tests/rls/fixtures/jwt.ts mintJWT`               | env `RLS_TEST_JWT_SECRET`                         | `TextEncoder().encode(process.env.RLS_TEST_JWT_SECRET)`                    | ✓ WIRED  | Line 24-32. Min-16-char guard.                                                                                                                                                                |
| `.github/workflows/ci.yml services.postgres`      | `tests/rls/global-setup.ts`                       | globalSetup applies migrations against the CI service container             | ✓ WIRED  | Workflow steps invoke `pnpm test:rls`, which triggers the harness's globalSetup.                                                                                                              |
| `eslint.config.mjs no-restricted-properties rule` | `lib/env.ts`                                      | rule `files` glob excludes lib/env.ts (only app/pages/src targeted)        | ✓ WIRED  | lib/env.ts is the only sanctioned `process.env` reader; outside the `files` glob, so allowed without explicit ignore. `pnpm lint` exits 0 confirms.                                            |
| Phase 6 `withTenantContext` request wrapper       | `lib/tenant/context.ts setTenantContext`          | (forward-looking) Phase 6 imports + calls inside server actions             | ⏸ DEFERRED | Phase 6's job. The signature contract is documented in JSDoc; `PgTransaction \| PgDatabase` typing supports both Phase 1 pen-test direct calls (note: pen tests use pg directly per Pitfall 5) and Phase 6 server-action transaction-wrapped calls. |

---

### Data-Flow Trace (Level 4)

For artifacts that render or pass dynamic data: data flows through verified paths to actually-functioning behavior. Level 4 analysis confirms tests prove behavior end-to-end, not just that types compile.

| Artifact                                | Data Variable / Flow                  | Source                                                            | Produces Real Data | Status     |
| --------------------------------------- | ------------------------------------- | ----------------------------------------------------------------- | ------------------ | ---------- |
| `lib/env.ts`                            | `env.DATABASE_URL`                    | `process.env.DATABASE_URL` validated through Zod                  | Yes                | ✓ FLOWING  |
| `lib/db/client.ts`                      | `sql` (postgres-js) / `db` (Drizzle)  | `env.DATABASE_URL`                                                | Yes                | ✓ FLOWING  |
| `lib/tenant/context.ts`                 | `tenantId` parameter → `set_config` call | Passed by caller (Phase 6) or by tests (Plan 06 pg-direct path) | Yes                | ✓ FLOWING  |
| `tests/rls/fixtures/tenants.ts`         | `seedTwoTenants` returns 4 UUID strings | `gen_random_uuid()::text` → `set_config` → INSERT                | Yes                | ✓ FLOWING (verified by 27/27 live tests; smoke also confirmed in Plan 06 SUMMARY: "Tenant A sees canaries: [{ id: ..., payload: 'A-payload' }]") |
| `db/migrations/0000_initial.sql` policies | Policy expression evaluates GUC         | `current_setting('app.tenant_id', true)::uuid`                  | Yes                | ✓ FLOWING (verified by cross-tenant SELECT/write tests asserting 0 rows for cross-tenant cases) |
| `tests/rls/index-scan.test.ts`          | EXPLAIN plan JSON                     | `EXPLAIN (FORMAT JSON) SELECT ...`                                | Yes                | ✓ FLOWING (live test confirms plan contains "Index Scan" + "rls_canary_tenant_idx") |

No HOLLOW or DISCONNECTED artifacts. Phase 1 is greenfield infrastructure; the only "rendering" surface is the pen-test suite's assertions, and those run live against real Postgres data.

---

### Behavioral Spot-Checks

| Behavior                                                        | Command                                                                                                                       | Result                                            | Status |
| --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ------ |
| TypeScript compiles strict                                      | `pnpm typecheck`                                                                                                              | exit 0                                            | ✓ PASS |
| Lint clean (excludes intentional fixture)                       | `pnpm lint`                                                                                                                   | exit 0                                            | ✓ PASS |
| Lint fixture rule fires (proves `no-restricted-*` rules work)   | `pnpm lint:fixture`                                                                                                           | exit 0 (inverted from eslint exit 1; 2 errors)    | ✓ PASS |
| Pen-test suite all green                                        | `pnpm test:rls`                                                                                                               | 7 files / 27 tests / 657ms — all pass             | ✓ PASS |
| FORCE RLS active on tenant + _rls_canary                        | `psql ... pg_class.relforcerowsecurity`                                                                                       | `_rls_canary \| t` AND `tenant \| t`              | ✓ PASS |
| Both expected policies exist                                    | `psql ... pg_policy`                                                                                                          | `tenant_self_filter` + `canary_tenant_isolation`  | ✓ PASS |
| `rls_canary_tenant_idx` index exists                            | `psql ... pg_indexes`                                                                                                         | present                                           | ✓ PASS |
| `app_user` is NOBYPASSRLS NOSUPERUSER                           | `psql ... pg_roles`                                                                                                           | `rolbypassrls=f, rolsuper=f`                      | ✓ PASS |
| Both migrations recorded in journal                              | `cat db/migrations/meta/_journal.json`                                                                                        | tags `0000_initial` + `0001_force_rls` present    | ✓ PASS |
| Docker container healthy                                        | `docker compose ps`                                                                                                           | `Up (healthy)`                                    | ✓ PASS |

All behavioral spot-checks pass live. Phase 1 is structurally operational at the local layer.

---

### Requirements Coverage

| Requirement | Source Plan(s)        | Description                                                                                                                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                             |
| ----------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TNT-01      | 01-04, 01-05          | `FORCE ROW LEVEL SECURITY` on every tenant-scoped table; even table owners are subject to policy                                                                                                                          | ✓ SATISFIED | `0001_force_rls.sql` contains both ALTERs; live `pg_class.relforcerowsecurity = t` for both `tenant` and `_rls_canary`; `tests/rls/setup.ts` beforeAll guards regression.                                                                                                |
| TNT-02      | 01-03, 01-08          | Read `tenant_id` from JWT app_metadata via `current_setting('app.tenant_id')` at every request boundary; service-role connections never bypass RLS for application traffic                                                | ✓ SATISFIED (with seam) | `lib/tenant/context.ts::setTenantContext` ships with documented Phase 6 contract; policies use `current_setting('app.tenant_id', true)::uuid`; ESLint rule + tests/rls/service-role-boundary.test.ts asserts `SUPABASE_SERVICE_ROLE_KEY` undefined. JWT app_metadata extraction lands in Phase 6 (Supabase Auth wiring). |
| TNT-03      | 01-04                 | `tenant_id` indexed on every tenant-scoped table                                                                                                                                                                          | ✓ SATISFIED | `db/schema/canary.ts` declares `index('rls_canary_tenant_idx').on(t.tenantId)`; `0000_initial.sql` emits CREATE INDEX; live pg_indexes confirms; `tests/rls/index-scan.test.ts` asserts EXPLAIN uses Index Scan via this index.                                              |
| TNT-04      | 01-06, 01-07          | CI pen-test suite at `tests/rls/` blocks merge on any cross-tenant query that succeeds                                                                                                                                    | ✓ SATISFIED (CI run pending human verification) | All 6 pen-test files exist + 1 env-boot test; 27/27 tests pass live. `.github/workflows/ci.yml` runs `pnpm test:rls` as a workflow step. The workflow has not yet been observed running on real GitHub Actions infrastructure (see `human_verification` block).                |
| TNT-05      | 01-04, 01-07          | Zero cross-tenant data egress paths                                                                                                                                                                                       | ✓ SATISFIED | Structural absence verified: no admin "view as another tenant" surface, no cross-tenant analytics path; `tests/rls/cross-tenant-select.test.ts` (4 it blocks) + `cross-tenant-write.test.ts` (4 it blocks) assert 0 rows / WITH CHECK rejection on every cross-tenant attempt. |
| TNT-06      | 01-07                 | No aggregate competitive-analytics surface                                                                                                                                                                               | ✓ SATISFIED | Structural absence verified at Phase 1 (no UI, no admin view, no analytics route). Phase 1 has no surface that could violate this; the architectural commitment is encoded in PROJECT.md / PITFALLS.md and structurally absent in code.                                  |
| CFG-02      | 01-01, 01-03          | Postgres 16+ via Supabase with Drizzle 0.45; `prepare: false` set for the Supabase pooler                                                                                                                                  | ✓ SATISFIED (Phase 1 contract; Supabase cloud project deferred to Phase 6) | docker-compose.yml uses `postgres:16-alpine`; package.json pins `drizzle-orm@0.45.2`; lib/db/client.ts has `prepare: false` (line 29). The "via Supabase" cloud-project portion deferred to Phase 6 per orchestrator's explicit acceptance note and CONTEXT D-07/D-08. |
| CFG-05      | 01-01, 01-02, 01-08   | Vercel encrypted env + t3-env runtime validation; no plaintext secrets in source                                                                                                                                          | ✓ SATISFIED (Phase 1 contract; Vercel encrypted env wiring deferred to Phase 6) | `lib/env.ts` t3-env validates required keys; `.env.local` gitignored; `.env.local.example` committed; ESLint rule blocks `process.env` from app paths; `tests/rls/env-boot.test.ts` (8 tests) proves boot-fail-closed semantics. Vercel encrypted env wiring lands in Phase 6 (no Vercel project at Phase 1). |

**No orphaned requirements.** All 8 Phase 1 IDs (TNT-01..06, CFG-02, CFG-05) are claimed by at least one plan and verified against actual code.

**Note on REQUIREMENTS.md status flags:** TNT-01..06 are marked `[x]` in REQUIREMENTS.md; CFG-05 is marked `[x]`; CFG-02 is still marked `[ ]` and the traceability table at the bottom of REQUIREMENTS.md says "Not started." This is a tracker-update gap — the Phase 1 contract for CFG-02 is satisfied (Postgres 16, Drizzle 0.45, prepare:false in code; verified live), but the cloud-Supabase portion is genuinely deferred to Phase 6, which is why the requirement is left unchecked overall. This is a documentation hygiene item, not a code gap.

---

### Anti-Patterns Found

| File / Concern               | Pattern                                                                                                  | Severity | Impact                                                                                                                                              |
| ---------------------------- | -------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none)                       | `react-scripts`, `prisma`, `mongo`, `tabula`, `drools`, `middleware.ts`, `revalidateTag(tag)` deprecated | -        | NOT FOUND — all CLAUDE.md anti-pattern dependencies are absent from package.json and code.                                                            |
| (none)                       | `drizzle-kit push` (Issue #3504 — silently drops RLS DDL)                                                | -        | NOT FOUND — package.json has only `db:migrate` (drizzle-kit migrate); no `db:push` script.                                                            |
| (none)                       | `localStorage` API-key pattern, browser-side LLM calls                                                   | -        | NOT FOUND — Phase 1 has no client/browser code; CLAUDE.md exclusion respected. (Legacy `src/App.js` excluded from tsconfig per design.)                |
| (none)                       | TODO / FIXME / XXX / HACK in Phase 1 code                                                                | -        | NOT FOUND — all "placeholder" mentions in `lib/`, `db/`, `tests/rls/` are technical references to Postgres 16 placeholder GUCs (legitimate documentation), not stub markers. |
| (none)                       | Stub returns: `return null`, `return {}`, `return []`                                                    | -        | NOT FOUND — every artifact has substantive logic; confirmed by 27/27 live tests passing (a stub would have failed at least one assertion).            |
| (none)                       | Hardcoded empty data flowing to user-visible output                                                      | -        | N/A — no UI surface in Phase 1.                                                                                                                       |

**Single source of truth check:** `grep -rn "app.tenant_id" lib/ db/ tests/` returns the literal in three logical groups exactly per Plan 03's contract:
1. `lib/tenant/context.ts` (the primitive — JSDoc + impl + constant; one file, multiple references for documentation)
2. `db/schema/{tenant,canary}.ts` (pgPolicy declarations on both tables)
3. `tests/rls/*` (pen tests + fixtures using the literal directly because of the driver split — pen tests use `pg`, not Drizzle's typed handle, per Pitfall 5)

The literal also appears in `db/migrations/0000_initial.sql` (drizzle-kit-generated; downstream of group 2 — this is the artifact, not a hand-written reference) and in CLAUDE.md / README.md (documentation). No source code outside the three sanctioned groups references the literal.

---

### Decision Honoring (CONTEXT.md D-01..D-13)

| ID    | Decision                                                                                                                                                                                | Status  | Evidence                                                                                                                                                                                       |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-01  | Pen tests in TS with Vitest + node-postgres + jose at tests/rls/                                                                                                                       | ✓ Honored | All 7 test files at tests/rls/; Vitest 4.1.5 in devDeps; pg 8.20.0 + jose 6.2.3 imported.                                                                                                       |
| D-02  | Service-role bypass guarded by env-var segregation + ESLint rule blocking SUPABASE_SERVICE_ROLE_KEY from app paths                                                                       | ✓ Honored | `eslint.config.mjs` + `app/.eslint-fixture.ts` + `tests/rls/service-role-boundary.test.ts` (3 it blocks).                                                                                       |
| D-03  | Pen-test coverage matrix: cross-select, cross-write, JWT tamper, service-role, GUC reset, index scan                                                                                    | ✓ Honored | All 6 test files exist with the required it blocks (4+4+3+3+3+2 = 19 cross-tenant + 8 env-boot = 27 total tests).                                                                                |
| D-04  | tenant + _rls_canary tables; tenant_kind enum BROKERAGE/RETAIL_LENDER/WHOLESALE_LENDER/SYSTEM                                                                                            | ✓ Honored | `db/schema/tenant.ts` + `db/schema/canary.ts`; pgEnum locked to all 4 values in order.                                                                                                          |
| D-05  | Self-filtering policy on tenant table itself                                                                                                                                            | ✓ Honored | `pgPolicy('tenant_self_filter', ...)` USING + WITH CHECK on `id = current_setting('app.tenant_id', true)::uuid`.                                                                                |
| D-06  | tenant_id indexed; pen test asserts EXPLAIN uses index scan                                                                                                                              | ✓ Honored | `index('rls_canary_tenant_idx')` declared + emitted; `tests/rls/index-scan.test.ts` asserts EXPLAIN.                                                                                              |
| D-07  | Plain Postgres 16 docker container (NOT Supabase CLI)                                                                                                                                   | ✓ Honored | `docker-compose.yml` uses `postgres:16-alpine`; no Supabase CLI invocation anywhere in scripts or workflows.                                                                                     |
| D-08  | prepare:false documented; Phase 6 wires real pooler                                                                                                                                     | ✓ Honored (Phase 1 contract) | `lib/db/client.ts:29` has `prepare: false`; JSDoc documents the Phase 6 Supabase posture migration as a config change, not a refactor.                                              |
| D-09  | GitHub Actions Postgres 16 service container; pnpm test:rls; fresh DB per CI run                                                                                                        | ✓ Honored | `.github/workflows/ci.yml` uses `postgres:16-alpine` service; `pnpm test:rls` step; service container is fresh per workflow run.                                                                  |
| D-10  | drizzle-kit only; RLS SQL via --custom migration                                                                                                                                         | ✓ Honored | drizzle-kit in devDeps; `0001_force_rls.sql` is the `--custom` migration containing the FORCE ALTERs; no Supabase CLI in migration loop.                                                          |
| D-11  | setTenantContext at lib/tenant/context.ts; set_config(..., is_local=true)                                                                                                                | ✓ Honored | `lib/tenant/context.ts:61` — `set_config('app.tenant_id', ${tenantId}, true)`. Third arg `true` is non-negotiable per Pitfall 3 — verified.                                                       |
| D-12  | t3-env at lib/env.ts; Zod 4; server schema separated; boot-fail-closed                                                                                                                  | ✓ Honored | `lib/env.ts` uses `@t3-oss/env-core` + Zod 4; `client: {}` separated from `server`; `emptyStringAsUndefined: true` enforces fail-closed; `tests/rls/env-boot.test.ts` proves it.               |
| D-13  | .env.local local; GitHub Actions secrets CI; Vercel encrypted env prod (Phase 6 wires)                                                                                                   | ⏸ Partially honored (deferred portion is Phase 6's job) | `.env.local` gitignored + `.env.local.example` committed; CI uses `env:` block with workflow-level fixture secrets (no `secrets.*` references yet — none required at Phase 1; the contract is explicit). Vercel encrypted env wiring deferred to Phase 6. |

---

### Forward-Looking Deferrals

These items are explicitly out of scope for Phase 1 and will land in later phases. Each is structurally accommodated (the seam exists; the contract is documented; later phases consume without rework).

| Deferral                                                              | Lands in    | Phase 1 accommodation                                                                                                                              |
| --------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| Real Supabase project (cloud)                                         | Phase 6     | `prepare: false` posture set; `DATABASE_URL` is the only switch needed.                                                                            |
| Supabase Auth + JWT signature verification (RS256 with JWKS)          | Phase 6     | Phase 1 uses jose-minted HS256 fixture JWTs; `mintJWT`/`forgeJWT`/`extractTenantIdFromJWT` API stays stable across the boundary.                    |
| `withTenantContext` request wrapper                                   | Phase 6     | `setTenantContext` primitive contract documented for that consumer; signature accepts `PgTransaction \| PgDatabase` for forward-compat.            |
| Vercel encrypted env wiring                                           | Phase 6     | t3-env contract is in place; CI uses workflow env block + (later) GitHub Actions secrets; Vercel env wiring is a UI/dashboard step.                |
| Inngest worker pool, Sentry, Axiom                                    | Phase 6     | Out of scope for Phase 1; PROJECT.md and the Phase 1 SUMMARY enumerate.                                                                            |
| Real domain tables (program, agency_rule, license, evaluation_event) | Phase 2+    | Schema layout (`db/schema/*.ts`) is the established pattern; pen-test harness extension protocol documented in Plan 06 SUMMARY.                     |
| Manual GitHub Actions CI run + intentional-regression demonstration    | User action | Plan 08 SUMMARY documents auto-approval under `--auto`; the human-verification items at the top of this report enumerate what's needed.            |
| Branch protection rules on `main` to require CI / verify check        | User action | Repo-admin step; Plan 08 SUMMARY flags as separate manual task.                                                                                     |

---

## Human Verification Required

Three items need human action before the Phase 1 antitrust safety net is fully operational at the merge-gate layer. The local code is structurally complete and verified.

### 1. Push the branch and confirm CI runs green

**Test:** `git push -u origin <current-branch>`; open the GitHub Actions tab; watch the "CI" workflow run on the push.
**Expected:** All 11 steps in `.github/workflows/ci.yml` complete green: checkout → pnpm + Node 20 → install → provision app_user → typecheck → lint → lint:fixture → migrate + GRANT → FORCE RLS gate → `pnpm test:rls`. Total runtime ~3-5 min including dep install.
**Why human:** GitHub Actions infrastructure cannot be invoked from this verification environment. Local smoke ran 27/27 tests green in 657ms; the CI workflow is a verbatim replication so green-on-CI is highly likely, but only a real push proves the merge gate operates correctly.

### 2. Demonstrate CI catches a deliberate regression

**Test:** Temporarily edit `tests/rls/cross-tenant-select.test.ts` and change the first `expect(rows).toHaveLength(0)` to `expect(rows).toHaveLength(1)` (a wrong assertion). Push that commit. Confirm the CI workflow turns red AND any draft PR's check is failing. Then revert with `git revert HEAD` and push; confirm CI turns green.
**Expected:** The "Run RLS pen-test suite" step fails on the wrong assertion; the PR check shows ❌; revert restores green.
**Why human:** The intentional-break-and-revert flow requires pushing real commits to a remote, watching CI react in real time, and judging that the failure surfaced is the expected one. Plan 08 SUMMARY documents this as the explicit Plan 08 Task 4 follow-up after the auto-approved checkpoint.

### 3. Configure branch protection rules on `main`

**Test:** Repository settings → Branches → Branch protection rules → main → check "Require status checks to pass before merging" and add "CI / verify" to the required checks list.
**Expected:** Future PRs cannot merge until the CI / verify check passes; even a maintainer cannot merge a red PR without explicit override.
**Why human:** Branch protection is a GitHub repo-admin operation outside the codebase. Without it, the CI workflow runs but does not actually block merge — a maintainer could merge a red PR. Plan 08 SUMMARY flags this as a separate manual user step.

---

## Gaps Summary

**No code-level gaps.** All 5 ROADMAP success criteria pass against the live codebase. Every Phase 1 requirement ID (TNT-01..06, CFG-02, CFG-05) is satisfied at the Phase 1 contract layer, with three cleanly-deferred items routed to Phase 6 (the cloud-Supabase project; `withTenantContext` request wrapper; Vercel encrypted env wiring). The deferrals are explicit and structurally accommodated — Phase 6 will swap config values, not refactor code.

The phase is structurally green at the local layer:
- 27/27 tests pass live in 657ms
- typecheck, lint, lint:fixture all exit 0
- live psql introspection confirms FORCE RLS, both policies, tenant_id index, and `app_user` NOBYPASSRLS NOSUPERUSER role
- The single-source-of-truth contract for `'app.tenant_id'` holds (three logical groups; no smell references)
- Anti-pattern check is clean (no react-scripts, no Prisma, no drizzle-kit push, no middleware.ts, no TODO/FIXME/HACK markers)

The three `human_verification` items are NOT gaps — they are normal steps in the Phase 1 closure that require pushing commits to a real GitHub repo, watching CI react, and configuring branch protection. The local smoke is comprehensive enough that CI is highly likely to succeed verbatim; this verification is gated on confirming the GitHub Actions side of the merge gate operates as specified.

---

## Bottom Line

**Phase 1 achieves the antitrust insurance policy goal at the local layer.** The Postgres database stands up with `FORCE ROW LEVEL SECURITY` on every tenant-scoped table (verified live: `pg_class.relforcerowsecurity = t` for both `tenant` and `_rls_canary`). The CI pen-test suite exists at `tests/rls/` (7 test files; 27 tests; D-03 coverage matrix complete) and is wired into `.github/workflows/ci.yml` to block merge on any pen-test failure. The `setTenantContext` primitive uses `set_config('app.tenant_id', $1, true)` (is_local=true non-negotiable). The single-source-of-truth contract for the GUC literal holds across the three sanctioned source groups. ESLint rules + the smoke fixture prove the structural defenses against `process.env` exfiltration and service-role import-path leaks fire today. The "forgot a `WHERE` clause" failure mode now manifests as no-rows-returned (verified by 8 cross-tenant SELECT/write tests), not as cross-tenant exfiltration.

The phase is ready to advance to `/gsd-verify-work` once the human verification items are completed (push + watch CI; demonstrate intentional-break catches; configure branch protection). The alternative — accepting the local verification as sufficient — is a documented option but leaves the actual GitHub merge gate unverified until first contact with real CI infrastructure.

---

_Verified: 2026-04-29_
_Verifier: Claude (gsd-verifier)_
