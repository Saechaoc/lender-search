---
phase: 01-tenant-isolation-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.json
  - .gitignore
  - .npmrc
  - docker-compose.yml
  - scripts/init-db.sh
  - vitest.config.ts
  - .env.local.example
  - drizzle.config.ts
  - README.md
autonomous: true
requirements:
  - CFG-02
  - CFG-05
threats:
  - T-01-07
must_haves:
  truths:
    - "pnpm is the package manager; npm/yarn paths are removed"
    - "All deps pinned to exact versions (no ^/~) so CI is reproducible"
    - "Local Postgres 16 is reachable via docker compose with a NOBYPASSRLS app_user role"
    - "Legacy react-scripts package.json is replaced — no carry-forward of CRA config"
  artifacts:
    - path: "package.json"
      provides: "TS project metadata, exact-pin deps, pnpm scripts (db:up/down/migrate/generate, test:rls, lint, typecheck)"
      contains: '"packageManager": "pnpm@'
    - path: "tsconfig.json"
      provides: "TypeScript 5.7 strict compiler config"
      contains: '"strict": true'
    - path: "docker-compose.yml"
      provides: "postgres:16-alpine with persistent volume + healthcheck + init script"
      contains: "postgres:16-alpine"
    - path: "scripts/init-db.sh"
      provides: "Provisions app_user role with NOBYPASSRLS NOSUPERUSER on first container boot"
      contains: "NOBYPASSRLS"
    - path: "vitest.config.ts"
      provides: "pool: forks, setupFiles, globalSetup wired (referenced files land in later plans)"
      contains: "pool: 'forks'"
    - path: ".gitignore"
      provides: "Ignores node_modules, .next, .turbo, dist, .env.local, build artifacts"
      contains: ".env.local"
    - path: ".env.local.example"
      provides: "Documents the env contract Plan 02 will validate"
      contains: "DATABASE_URL=postgresql://app_user"
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit config pointing at db/schema/index.ts and db/migrations/"
      contains: "dialect: 'postgresql'"
  key_links:
    - from: "package.json scripts"
      to: "docker-compose.yml"
      via: "db:up runs `docker compose up -d postgres`"
      pattern: "docker compose up"
    - from: "vitest.config.ts"
      to: "tests/rls/setup.ts (created Plan 06)"
      via: "setupFiles array references the path"
      pattern: "tests/rls/setup.ts"
    - from: "drizzle.config.ts"
      to: "db/schema/index.ts (created Plan 04)"
      via: "schema field points at it"
      pattern: "./db/schema/index.ts"
---

<objective>
Replace the legacy `react-scripts` workspace with a TypeScript 5.7 monorepo-shape (single package) tooling baseline that the rest of Phase 1 builds on. Stand up `pnpm` as the package manager, pin every dep to an exact version, declare a docker-compose Postgres 16 service with a NOBYPASSRLS app_user, and commit the empty Vitest + Drizzle Kit config shells the later plans populate. No Postgres DDL, no RLS policies, no pen tests yet — those land in Waves 2-3.

Purpose: Establish the foundation that every later plan in this phase (and every phase after) depends on. Pin tools so CI is reproducible. Provision the local DB the next two waves of plans connect to.

Output: A repo whose `pnpm install && pnpm db:up && pnpm typecheck` runs cleanly even though no application code exists yet. Drizzle Kit recognizes the config; Vitest recognizes the config; Postgres 16 is listening on 5432 with `app_user` provisioned.
</objective>

<execution_context>
This plan is the workspace bootstrap. Plans 02 (env), 03 (db client + tenant context), 04 (schema), 05 (force RLS migration), 06 (pen-test harness), 07 (pen tests), and 08 (CI + ESLint + verification) all depend on this plan landing first.

The legacy `src/App.js` and its `react-scripts` config are explicitly NOT touched by this plan beyond replacing the root `package.json`. The legacy file remains in `src/` and will be relocated/deleted in Phase 6 when the Next.js app skeleton lands. CONTEXT.md is silent on cleanup; defer per RESEARCH §Open Questions Q1.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tenant-isolation-foundation/01-CONTEXT.md
@.planning/phases/01-tenant-isolation-foundation/01-RESEARCH.md
@.planning/phases/01-tenant-isolation-foundation/01-VALIDATION.md
@CLAUDE.md
@package.json
@.gitignore
</context>

<truths>
- pnpm is the package manager; the legacy npm `package-lock.json` and `node_modules/` from CRA are deleted before the new install runs
- All package versions pinned exactly (no `^` or `~`) so CI matches local exactly — `drizzle-orm@0.45.2`, `drizzle-kit@0.31.10`, `postgres@3.4.9`, `pg@8.20.0`, `vitest@4.1.5`, `jose@6.2.3`, `zod@4.4.1`, `@t3-oss/env-core@0.13.11`, `dotenv@17.4.2`, `tsx@4.21.0`, `eslint@10.2.1`, `typescript@5.7.3`
- Phase 1 has NO Next.js, NO React 19, NO @anthropic-ai/sdk, NO Inngest, NO shadcn/ui, NO Sentry/Axiom — including any of those is scope creep (RESEARCH §Standard Stack)
- drizzle-kit migrate (file-based migrations) is the only path; never drizzle-kit push (silently drops RLS DDL — Issue #3504)
- Postgres 16+ via Supabase is the contract (CFG-02); Phase 1 ships the contract via plain Postgres 16 docker (D-07) — Phase 6 wires the real Supabase project
- Local dev secrets in `.env.local` (gitignored); committed `.env.local.example` documents the schema Plan 02 enforces
</truths>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy react-scripts package.json with pinned-deps TypeScript baseline + pnpm scripts</name>
  <files>
    - package.json
    - pnpm-workspace.yaml
    - .npmrc
    - .gitignore
  </files>
  <read_first>
    - /Users/chrissaechao/IdeaProjects/lender-search/package.json (current react-scripts state — must be replaced wholesale)
    - /Users/chrissaechao/IdeaProjects/lender-search/.gitignore (current — extend, do not rewrite)
    - /Users/chrissaechao/IdeaProjects/lender-search/.planning/phases/01-tenant-isolation-foundation/01-RESEARCH.md §"Standard Stack" + §"Installation" (exact version pins)
    - /Users/chrissaechao/IdeaProjects/lender-search/.planning/phases/01-tenant-isolation-foundation/01-CONTEXT.md §D-07, D-09, D-10 (docker postgres, GH Actions CI, drizzle-kit migrate)
    - /Users/chrissaechao/IdeaProjects/lender-search/CLAUDE.md §"Technology Stack" (locked stack — TS 5.7, Drizzle 0.45, no Prisma, pnpm)
  </read_first>
  <action>
    Replace the existing `/Users/chrissaechao/IdeaProjects/lender-search/package.json` (CRA/react-scripts) with the following exact content. All deps pinned to exact versions per RESEARCH §Standard Stack. Do not run `pnpm install` yet — Task 2 reorders the install relative to dir structure creation.

    ```json
    {
      "name": "lender-search",
      "version": "0.1.0",
      "private": true,
      "type": "module",
      "packageManager": "pnpm@9.15.0",
      "engines": {
        "node": ">=20.9.0",
        "pnpm": ">=9.0.0"
      },
      "scripts": {
        "db:up": "docker compose up -d postgres",
        "db:down": "docker compose down",
        "db:logs": "docker compose logs -f postgres",
        "db:reset": "docker compose down -v && docker compose up -d postgres",
        "db:generate": "drizzle-kit generate",
        "db:migrate": "drizzle-kit migrate",
        "test:rls": "vitest run --config vitest.config.ts",
        "test:rls:watch": "vitest --config vitest.config.ts",
        "lint": "eslint .",
        "typecheck": "tsc --noEmit"
      },
      "dependencies": {
        "@t3-oss/env-core": "0.13.11",
        "drizzle-orm": "0.45.2",
        "postgres": "3.4.9",
        "zod": "4.4.1"
      },
      "devDependencies": {
        "@types/node": "20.19.4",
        "@types/pg": "8.15.5",
        "dotenv": "17.4.2",
        "drizzle-kit": "0.31.10",
        "eslint": "10.2.1",
        "jose": "6.2.3",
        "pg": "8.20.0",
        "tsx": "4.21.0",
        "typescript": "5.7.3",
        "typescript-eslint": "8.46.0",
        "vitest": "4.1.5"
      }
    }
    ```

    Create `/Users/chrissaechao/IdeaProjects/lender-search/pnpm-workspace.yaml` with this exact content (single-package workspace; future-proofs adding `apps/web` in Phase 6 without restructuring):
    ```yaml
    packages:
      - "."
    ```

    Create `/Users/chrissaechao/IdeaProjects/lender-search/.npmrc` with this exact content (forces exact pins on `pnpm add` so accidental `^` ranges cannot creep in):
    ```
    save-exact=true
    save-prefix=
    auto-install-peers=true
    ```

    Read existing `/Users/chrissaechao/IdeaProjects/lender-search/.gitignore` and APPEND (do not replace) the following block at the bottom. Existing entries cover `node_modules`, `.env.local`, `build/` already — do not duplicate them. New entries:
    ```
    # TypeScript build output
    dist/
    *.tsbuildinfo

    # Next.js (forward-looking; arrives Phase 6)
    .next/
    out/

    # Turborepo (forward-looking)
    .turbo/

    # Vitest cache
    .vitest/

    # pnpm
    .pnpm-store/
    ```

    Do NOT delete `package-lock.json` or `node_modules/` in this task — Task 2 handles the install transition cleanly.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.packageManager!=='pnpm@9.15.0')process.exit(1); if(p.dependencies['drizzle-orm']!=='0.45.2')process.exit(2); if(p.devDependencies['vitest']!=='4.1.5')process.exit(3); if(p.devDependencies['typescript']!=='5.7.3')process.exit(4); console.log('package.json ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"packageManager": "pnpm@9.15.0"` (exact line)
    - `package.json` `dependencies."drizzle-orm"` equals `"0.45.2"` (no caret/tilde)
    - `package.json` `devDependencies."drizzle-kit"` equals `"0.31.10"`
    - `package.json` `devDependencies."vitest"` equals `"4.1.5"`
    - `package.json` `devDependencies."typescript"` equals `"5.7.3"`
    - `package.json` `devDependencies."pg"` equals `"8.20.0"` AND `dependencies."postgres"` equals `"3.4.9"` (both drivers present per RESEARCH Pitfall 5)
    - `package.json` scripts contains `db:migrate`, `db:generate`, `test:rls`, `lint`, `typecheck` (verify with `node -e "console.log(Object.keys(require('./package.json').scripts))"`)
    - `package.json` does NOT contain `react-scripts`, `react`, `react-dom`, or `web-vitals` (verify with `grep -L 'react-scripts' package.json`)
    - `pnpm-workspace.yaml` exists at repo root and contains `packages:` line
    - `.npmrc` exists and contains `save-exact=true`
    - `.gitignore` contains `dist/` AND `.next/` AND `.pnpm-store/` (verify: `grep -q '^dist/$' .gitignore && grep -q '^\.next/$' .gitignore && grep -q '^\.pnpm-store/$' .gitignore`)
  </acceptance_criteria>
  <done>
    The repo root has a TypeScript-shaped `package.json` with all Phase 1 deps at exact versions, pnpm declared as packageManager, and supporting workspace/npmrc/gitignore files in place. Legacy CRA deps are gone from manifest (lockfile cleanup happens in Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tsconfig.json + drizzle.config.ts + vitest.config.ts (empty shells) and run pnpm install</name>
  <files>
    - tsconfig.json
    - drizzle.config.ts
    - vitest.config.ts
    - .env.local.example
  </files>
  <read_first>
    - /Users/chrissaechao/IdeaProjects/lender-search/package.json (just written by Task 1)
    - /Users/chrissaechao/IdeaProjects/lender-search/.planning/phases/01-tenant-isolation-foundation/01-RESEARCH.md §"Pattern 4: Vitest Pen-Test Harness" (vitest config shape) and §"Code Examples / Drizzle config" (drizzle.config.ts shape)
    - /Users/chrissaechao/IdeaProjects/lender-search/.planning/phases/01-tenant-isolation-foundation/01-CONTEXT.md §D-09, §D-10 (CI + drizzle-kit only)
  </read_first>
  <action>
    Create `/Users/chrissaechao/IdeaProjects/lender-search/tsconfig.json` with this exact content (TS 5.7 strict, ES2022, NodeNext for pnpm + ESM):
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "lib": ["ES2022"],
        "module": "NodeNext",
        "moduleResolution": "NodeNext",
        "strict": true,
        "noUncheckedIndexedAccess": true,
        "noImplicitOverride": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true,
        "resolveJsonModule": true,
        "declaration": false,
        "sourceMap": true,
        "incremental": true,
        "tsBuildInfoFile": "./.tsbuildinfo",
        "outDir": "./dist",
        "rootDir": "."
      },
      "include": [
        "lib/**/*.ts",
        "db/**/*.ts",
        "tests/**/*.ts",
        "drizzle.config.ts",
        "vitest.config.ts"
      ],
      "exclude": [
        "node_modules",
        "dist",
        "src",
        "build",
        ".next"
      ]
    }
    ```

    The `"src"` exclusion isolates the legacy `src/App.js` prototype from the new toolchain (per RESEARCH Q1 recommendation — defer cleanup to Phase 6).

    Create `/Users/chrissaechao/IdeaProjects/lender-search/drizzle.config.ts` with this exact content (verbatim from RESEARCH §"Code Examples / Drizzle config"):
    ```typescript
    import { defineConfig } from 'drizzle-kit';
    import 'dotenv/config';

    if (!process.env.DATABASE_URL) {
      throw new Error('DATABASE_URL is required');
    }

    export default defineConfig({
      dialect: 'postgresql',
      schema: './db/schema/index.ts',
      out: './db/migrations',
      dbCredentials: {
        url: process.env.DATABASE_URL,
      },
      verbose: true,
      strict: true,
    });
    ```

    Note: `drizzle-kit` will fail to load this until `db/schema/index.ts` exists (created in Plan 04). That is intentional — only `pnpm db:generate` and `pnpm db:migrate` need that path; `pnpm install` and `pnpm typecheck` do not.

    Create `/Users/chrissaechao/IdeaProjects/lender-search/vitest.config.ts` with this exact content (per RESEARCH §"Pattern 4"):
    ```typescript
    import { defineConfig } from 'vitest/config';

    export default defineConfig({
      test: {
        pool: 'forks',
        isolate: true,
        globalSetup: ['./tests/rls/global-setup.ts'],
        setupFiles: ['./tests/rls/setup.ts'],
        sequence: { concurrent: false },
        testTimeout: 10_000,
        hookTimeout: 10_000,
        include: ['tests/rls/**/*.test.ts'],
      },
    });
    ```

    The referenced files (`tests/rls/global-setup.ts`, `tests/rls/setup.ts`) are created in Plan 06. Vitest will not be invoked here — they only need to exist when `pnpm test:rls` runs.

    Create `/Users/chrissaechao/IdeaProjects/lender-search/.env.local.example` with this exact content (committed to repo; documents the contract Plan 02 enforces):
    ```
    # Local Postgres connection string (docker compose provisions app_user)
    DATABASE_URL=postgresql://app_user:app_user_password@localhost:5432/lender_search_dev

    # Pen-test JWT signing secret (HS256 fixture; min 16 chars)
    RLS_TEST_JWT_SECRET=local-dev-fixture-secret-change-me

    # Phase 1: optional. Phase 6 wires the real Supabase project and tightens to required.
    # SUPABASE_SERVICE_ROLE_KEY=
    ```

    Now perform the install transition:
    1. Delete the legacy CRA artifacts: `rm -rf node_modules package-lock.json`
    2. Run: `pnpm install` (uses the new package.json — generates `pnpm-lock.yaml`)
    3. Verify: `pnpm typecheck` exits 0 (compiles config files; no app code yet)

    If `pnpm install` fails, do NOT proceed — diagnose. Likely cause is a typo in package.json or network/registry access.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false 2>&1 | tail -5 && test -f /Users/chrissaechao/IdeaProjects/lender-search/pnpm-lock.yaml && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.json` exists at repo root and contains `"strict": true` AND `"target": "ES2022"` AND `"module": "NodeNext"`
    - `tsconfig.json` `exclude` array contains `"src"` (legacy prototype isolation)
    - `drizzle.config.ts` exists at repo root and contains `dialect: 'postgresql'` AND `schema: './db/schema/index.ts'` AND `out: './db/migrations'`
    - `vitest.config.ts` exists at repo root and contains `pool: 'forks'` AND `globalSetup: ['./tests/rls/global-setup.ts']` AND `setupFiles: ['./tests/rls/setup.ts']`
    - `.env.local.example` exists at repo root and contains `DATABASE_URL=postgresql://app_user`
    - `pnpm-lock.yaml` exists at repo root after `pnpm install` completes
    - `node_modules/` has been refreshed (legacy CRA `react-scripts` is gone): `ls node_modules/.pnpm 2>/dev/null | head -1` returns lines (pnpm-style symlinks present)
    - `package-lock.json` is NOT present at repo root (legacy npm lockfile deleted)
    - `pnpm typecheck` exits with code 0
  </acceptance_criteria>
  <done>
    TypeScript, Drizzle Kit, and Vitest config shells are in place. pnpm-managed `node_modules/` is fresh with all Phase 1 deps at the pinned versions. The legacy npm lockfile is gone. `pnpm typecheck` passes against the (currently empty) `lib/` and `db/` and `tests/` trees.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create docker-compose.yml + scripts/init-db.sh + README local-dev section</name>
  <files>
    - docker-compose.yml
    - scripts/init-db.sh
    - README.md
  </files>
  <read_first>
    - /Users/chrissaechao/IdeaProjects/lender-search/.planning/phases/01-tenant-isolation-foundation/01-RESEARCH.md §"Pattern 7: Postgres 16 Local Dev + CI" (verbatim docker-compose.yml + init-db.sh)
    - /Users/chrissaechao/IdeaProjects/lender-search/.planning/phases/01-tenant-isolation-foundation/01-CONTEXT.md §D-07 (plain Docker Postgres at Phase 1) and §D-08 (direct connection only)
    - /Users/chrissaechao/IdeaProjects/lender-search/README.md (current — extend, do not rewrite)
  </read_first>
  <action>
    Create `/Users/chrissaechao/IdeaProjects/lender-search/docker-compose.yml` with this exact content (verbatim from RESEARCH §"Pattern 7"):
    ```yaml
    services:
      postgres:
        image: postgres:16-alpine
        container_name: lender-search-pg
        environment:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lender_search_dev
        ports:
          - "5432:5432"
        volumes:
          - lender-search-pg-data:/var/lib/postgresql/data
          - ./scripts/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U postgres"]
          interval: 5s
          timeout: 3s
          retries: 5

    volumes:
      lender-search-pg-data:
    ```

    Create directory `/Users/chrissaechao/IdeaProjects/lender-search/scripts/` (if missing) and create `/Users/chrissaechao/IdeaProjects/lender-search/scripts/init-db.sh` with this exact content (verbatim from RESEARCH §"Pattern 7"):
    ```bash
    #!/usr/bin/env bash
    set -e
    psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<-EOSQL
      -- Phase 1 uses gen_random_uuid() built into Postgres 13+. pgcrypto NOT required.
      CREATE ROLE app_user LOGIN PASSWORD 'app_user_password' NOBYPASSRLS NOSUPERUSER;
      GRANT CONNECT ON DATABASE lender_search_dev TO app_user;
      GRANT USAGE ON SCHEMA public TO app_user;
      GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO app_user;
      GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO app_user;
      ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO app_user;
      ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT USAGE, SELECT ON SEQUENCES TO app_user;
    EOSQL
    ```

    Make the init script executable: `chmod +x /Users/chrissaechao/IdeaProjects/lender-search/scripts/init-db.sh`

    The `NOBYPASSRLS NOSUPERUSER` clauses are non-negotiable — pen tests connect as this role and rely on it being subject to RLS (RESEARCH Pitfall 4).

    Read existing `/Users/chrissaechao/IdeaProjects/lender-search/README.md` and APPEND a new section after the existing content:
    ```markdown

    ## Local Development (Phase 1+)

    Phase 1 introduces the Postgres tenant-isolation foundation. To run the pen-test suite locally:

    ```bash
    # 1. Install dependencies (pnpm required — npm is not supported)
    pnpm install

    # 2. Bring up Postgres 16 (provisions NOBYPASSRLS app_user role on first boot)
    pnpm db:up

    # 3. Copy the env template and edit if needed
    cp .env.local.example .env.local

    # 4. Apply migrations (Plan 04+05 land them; this command will work once they exist)
    pnpm db:migrate

    # 5. Run the RLS pen-test suite
    pnpm test:rls
    ```

    To reset the local database (drops the docker volume and re-runs the init script):
    ```bash
    pnpm db:reset
    ```

    The init script provisions `app_user` (NOBYPASSRLS, NOSUPERUSER) — pen tests connect as this user so that `FORCE ROW LEVEL SECURITY` is meaningful. **Never connect tests as the `postgres` superuser; the pen-test setup file aborts the suite if it detects a privileged connection.**

    ### Run before merging any PR

    ```bash
    pnpm test:rls
    ```

    CI fails if any pen test fails. RLS regressions are treated as security incidents (see `CLAUDE.md` § Conventions and `.planning/research/PITFALLS.md` § 3.5).
    ```
  </action>
  <verify>
    <automated>test -f /Users/chrissaechao/IdeaProjects/lender-search/docker-compose.yml && test -x /Users/chrissaechao/IdeaProjects/lender-search/scripts/init-db.sh && grep -q 'postgres:16-alpine' /Users/chrissaechao/IdeaProjects/lender-search/docker-compose.yml && grep -q 'NOBYPASSRLS' /Users/chrissaechao/IdeaProjects/lender-search/scripts/init-db.sh && grep -q 'NOSUPERUSER' /Users/chrissaechao/IdeaProjects/lender-search/scripts/init-db.sh && grep -q 'pnpm test:rls' /Users/chrissaechao/IdeaProjects/lender-search/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` exists and contains `postgres:16-alpine` (exact image tag)
    - `docker-compose.yml` contains `lender-search-pg-data:/var/lib/postgresql/data` (named volume mount)
    - `docker-compose.yml` contains `./scripts/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro` (init script mount)
    - `scripts/init-db.sh` exists with executable bit set: `test -x scripts/init-db.sh`
    - `scripts/init-db.sh` contains `CREATE ROLE app_user LOGIN PASSWORD 'app_user_password' NOBYPASSRLS NOSUPERUSER`
    - `scripts/init-db.sh` does NOT use pgcrypto (gen_random_uuid is core in Postgres 13+): `! grep -q pgcrypto scripts/init-db.sh`
    - `README.md` contains a `## Local Development` section AND a `pnpm db:up` instruction AND a `pnpm test:rls` instruction
    - Smoke test: `docker compose up -d postgres` followed by `docker compose exec postgres pg_isready -U postgres` returns "accepting connections" within 30 seconds (NOTE: this requires Docker; if Docker unavailable in executor env, document the failure mode and proceed — the file contents are independently verifiable above)
  </acceptance_criteria>
  <done>
    `docker compose up -d postgres` brings up Postgres 16 with the `app_user` role provisioned (NOBYPASSRLS NOSUPERUSER). README documents the local-dev flow including the `pnpm test:rls` PR gate. The init script is permission-bit executable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev environment → docker postgres | Local-only; `app_user/app_user_password` is a docker-network credential, never reaches prod |
| repo source → CI | GitHub Actions reads pinned versions from package.json + pnpm-lock.yaml |

## STRIDE Threat Register (this plan only — full Phase 1 register spans plans 02-08)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | `.env.local`, `package.json`, lockfiles | mitigate | `.gitignore` covers `.env.local`; `.env.local.example` documents required keys without values; `.npmrc save-exact=true` prevents drift on `pnpm add`. Plan 02 layers t3-env runtime validation on top. |
</threat_model>

<verification>
After all three tasks land:

1. **Workspace shape:** `pnpm install --frozen-lockfile=false` exits 0; `pnpm typecheck` exits 0; `pnpm-lock.yaml` is committable.
2. **Docker stack:** `pnpm db:up` brings up Postgres 16 with `app_user` provisioned. `docker compose exec postgres psql -U postgres -d lender_search_dev -c "SELECT rolbypassrls FROM pg_roles WHERE rolname='app_user'"` returns `f` (NOBYPASSRLS confirmed).
3. **Hygiene:** `git status` shows the new files staged; `package-lock.json` and `node_modules` (the legacy CRA versions) are gone.

This plan does not run pen tests — those land in Plan 07. The verification here is "the workspace boots."
</verification>

<success_criteria>
- `package.json` is a TypeScript project with all Phase 1 deps pinned exactly (no `^/~`)
- `pnpm install` succeeds and produces `pnpm-lock.yaml`; `pnpm typecheck` passes
- `docker compose up -d postgres` brings up Postgres 16 with NOBYPASSRLS `app_user` role
- README documents the local-dev flow including `pnpm test:rls`
- No CRA / `react-scripts` artifacts remain in the manifest or lockfile (legacy `src/App.js` is excluded from tsconfig but not deleted — Phase 6 owns cleanup)
- Plan 02 (env validation) and Plan 03 (db client + tenant context) can begin once this plan lands
</success_criteria>

<output>
After completion, create `.planning/phases/01-tenant-isolation-foundation/01-01-SUMMARY.md` documenting:
- Final pinned versions (in case any minor drift was needed)
- Whether docker availability was confirmed in the executor env (or deferred to user verification)
- Any deviations from the plan (e.g., if Node 20.9+ wasn't available)
</output>
