---
phase: 01-tenant-isolation-foundation
plan: 08
type: execute
wave: 4
depends_on: ["01-07"]
files_modified:
  - eslint.config.mjs
  - app/.eslint-fixture.ts
  - .github/workflows/ci.yml
  - README.md
autonomous: false
requirements:
  - TNT-02
  - CFG-05
threats:
  - T-01-04
  - T-01-07
must_haves:
  truths:
    - "ESLint flat config (eslint.config.mjs) blocks `process.env.SUPABASE_SERVICE_ROLE_KEY` reads from app paths (`app/**`, `pages/**`, `src/**`) and blocks `service-role*`/`admin-db*` import patterns"
    - "ESLint flat config blocks ALL `process.env` reads from app paths (defense-in-depth — single source of truth is `lib/env.ts`)"
    - "ESLint allows `process.env` access in `lib/env.ts`, `db/migrations/**`, `tests/**`, `scripts/**` because those are the env-handling boundary"
    - "`app/.eslint-fixture.ts` is a static smoke test that exercises the rule — it intentionally violates and is rejected by `pnpm lint` (CI fails the workflow on lint errors)"
    - "GitHub Actions CI uses postgres:16-alpine service container, applies migrations as superuser, grants DML to app_user, then runs `pnpm test:rls` and `pnpm lint`"
    - "concurrency cancel-in-progress on PR so the latest push wins; matrix avoided (single Node version + single Postgres version per CFG-02)"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Flat ESLint config with no-restricted-properties + no-restricted-imports rules + typescript-eslint base"
      contains: "no-restricted-imports"
    - path: "app/.eslint-fixture.ts"
      provides: "Decoy app file containing forbidden process.env access; the lint command must reject it"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow: services.postgres:16, pnpm install, drizzle-kit migrate, pnpm test:rls, pnpm lint, pnpm typecheck"
      contains: "pnpm test:rls"
    - path: "README.md"
      provides: "Documents PR gate: pnpm test:rls + pnpm lint + pnpm typecheck must pass"
      contains: "PR gate"
  key_links:
    - from: ".github/workflows/ci.yml services.postgres"
      to: "tests/rls/global-setup.ts (Plan 06)"
      via: "globalSetup applies migrations against the CI service container"
      pattern: "postgres:16"
    - from: "eslint.config.mjs no-restricted-properties rule"
      to: "lib/env.ts (Plan 02)"
      via: "rule excludes lib/env.ts from the ban — it's the only sanctioned env-reader"
      pattern: "lib/env.ts"
---

<objective>
Land the GitHub Actions CI workflow that enforces the merge gate (`pnpm test:rls` + `pnpm lint` + `pnpm typecheck` must all pass on every PR), the ESLint flat config that blocks `SUPABASE_SERVICE_ROLE_KEY` and `process.env` reads from app paths, and a static smoke fixture that exercises the rule. Then run a final end-to-end smoke from a fresh state to verify Phase 1's KPIs: clean `pnpm db:reset && pnpm drizzle-kit migrate && pnpm test:rls` exits 0 in <60 seconds; CI reproduces the same flow.

Purpose: Close TNT-02 (service-role boundary) and CFG-05 (no plaintext secrets) with the structural defenses they need beyond Phase 1's current code surface (which has no `app/` directory yet). The ESLint rule ships now so Phase 6's first PR introducing `app/` is automatically gated. CI replicates the local flow exactly so a green local suite reliably means a green PR.

Output: Three new files (eslint config, fixture, ci workflow) + a README PR-gate section. A checkpoint at the end pauses for the user to push the branch and confirm CI runs green.
</objective>

<execution_context>
This is Wave 4 — depends on Plan 07 (the pen tests CI runs). After this plan, the phase is structurally complete: every requirement maps to a green test or a structural absence (TNT-06 has no surface to test).

The ESLint rule's `app/` glob is forward-looking — Phase 6 creates the first `app/` directory when Next.js lands. Plan 08 ships the rule and a single-file smoke fixture (`app/.eslint-fixture.ts`) so the rule's effect is verifiable today. The fixture is intentionally NOT compiled by `tsc` (excluded from tsconfig include via the leading dot) but IS linted by `pnpm lint` — and the lint MUST fail because the fixture violates the rule.

After CI is wired, this plan ends with a `checkpoint:human-verify` task: push the branch, watch the CI workflow run, confirm it gates merge correctly. The checkpoint is the only non-autonomous task in Phase 1.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tenant-isolation-foundation/01-CONTEXT.md
@.planning/phases/01-tenant-isolation-foundation/01-RESEARCH.md
@.planning/phases/01-tenant-isolation-foundation/01-VALIDATION.md
@CLAUDE.md
@package.json
@docker-compose.yml
@scripts/init-db.sh
@tests/rls/global-setup.ts
@tests/rls/setup.ts
@README.md
</context>

<truths>
- ESLint flat config (`eslint.config.mjs`) is the modern format (ESLint 9+; we're on 10.2.1 per Plan 01) — RESEARCH §"State of the Art" + §"Pattern 5"
- The rule blocks `process.env` access from `app/**`, `pages/**`, `src/**` (forward-looking; only `app/.eslint-fixture.ts` exists in Phase 1) — single source of truth is `lib/env.ts`
- The rule excludes `lib/env.ts`, `db/migrations/**`, `tests/**`, `scripts/**` because those are the sanctioned env-handling boundary
- The rule additionally bans `service-role*` / `admin-db*` import patterns from app paths
- `pnpm lint` rejects the fixture; CI fails the workflow on any lint error
- CI service container Postgres 16 starts empty; globalSetup migrates and grants; pen tests assert against the result
- The CI step ordering is non-negotiable: provision app_user → migrate as postgres → grant DML to app_user → run pen tests as app_user (RESEARCH §"Pattern 7")
- concurrency.cancel-in-progress on PR: pushing a new commit cancels the prior workflow run (saves CI minutes; the latest commit's run is what matters)
</truths>

<tasks>

<task type="auto">
  <name>Task 1: Create eslint.config.mjs (flat config blocking SUPABASE_SERVICE_ROLE_KEY + process.env from app paths)</name>
  <files>
    - eslint.config.mjs
    - app/.eslint-fixture.ts
  </files>
  <read_first>
    - /Users/chrissaechao/IdeaProjects/lender-search/.planning/phases/01-tenant-isolation-foundation/01-RESEARCH.md §"Pattern 5: Service-Role Boundary" (verbatim eslint.config.js block — adapt to .mjs flat config) AND §"State of the Art" (ESLint 10 flat config)
    - /Users/chrissaechao/IdeaProjects/lender-search/.planning/phases/01-tenant-isolation-foundation/01-CONTEXT.md §D-02 (env-var segregation + ESLint rule)
    - /Users/chrissaechao/IdeaProjects/lender-search/lib/env.ts (Plan 02 — the only sanctioned env-reader; ESLint must allow it)
    - /Users/chrissaechao/IdeaProjects/lender-search/package.json (eslint@10.2.1 + typescript-eslint@8.46.0 from Plan 01)
  </read_first>
  <action>
    Create `/Users/chrissaechao/IdeaProjects/lender-search/eslint.config.mjs` with this exact content (flat config; ESM):

    ```javascript
    /**
     * ESLint flat config (Phase 1 / D-02).
     *
     * Three load-bearing rules:
     *   1. `no-restricted-properties` blocks `process.env` reads from app paths.
     *      Single source of truth is `lib/env.ts` (t3-env validated).
     *   2. `no-restricted-imports` blocks `service-role*` / `admin-db*` patterns.
     *      Service-role connections must NEVER appear in the application graph.
     *   3. typescript-eslint recommended rules for type-aware static analysis.
     *
     * Forward-looking: `app/` does not exist until Phase 6. Plan 08 ships the
     * rule + a smoke fixture (`app/.eslint-fixture.ts`) that exercises it; the
     * lint command rejects the fixture, proving the rule fires.
     *
     * Sanctioned env-reader exclusions: `lib/env.ts` (the t3-env schema),
     * `db/migrations/**` (drizzle artifacts; no logic), `tests/**` (pen-test
     * env access for stubEnv etc.), `scripts/**` (local-dev maintenance).
     */
    import tseslint from 'typescript-eslint';

    export default tseslint.config(
      // Base TS recommended rules.
      ...tseslint.configs.recommended,

      // Phase 1 application-path guards (forward-looking; app/ arrives Phase 6).
      {
        files: [
          'app/**/*.ts',
          'app/**/*.tsx',
          'pages/**/*.ts',
          'pages/**/*.tsx',
          'src/**/*.ts',
          'src/**/*.tsx',
        ],
        rules: {
          // Block all `process.env` access from app paths.
          // The single sanctioned env-reader is lib/env.ts (t3-env).
          'no-restricted-properties': [
            'error',
            {
              object: 'process',
              property: 'env',
              message:
                'Read env vars from `lib/env.ts` (t3-env) only. Direct `process.env` access in app paths bypasses runtime validation (CFG-05).',
            },
          ],

          // Block service-role / admin-db import patterns.
          // Migration scripts and CI tools may use them; app code may not.
          'no-restricted-imports': [
            'error',
            {
              patterns: [
                {
                  group: ['**/service-role*', '**/admin-db*'],
                  message:
                    'service_role / admin DB clients must not be imported from app paths. Only migration scripts and CI tools may use them (TNT-02 / D-02).',
                },
              ],
            },
          ],
        },
      },

      // Global ignores (paths the rule exclusions don't reach).
      {
        ignores: [
          'node_modules/**',
          'dist/**',
          '.next/**',
          'build/**',
          'src/App.js',  // legacy CRA prototype — Phase 6 deletes
          'src/App.test.js',
          'src/index.js',
          'src/setupTests.js',
          'src/reportWebVitals.js',
          'public/**',
          'coverage/**',
          '.tsbuildinfo',
        ],
      },
    );
    ```

    Now create the smoke fixture that exercises the rule. The fixture INTENTIONALLY violates the rule so `pnpm lint` rejects it. This is how we prove the rule fires today even though no real `app/` code exists.

    Create `/Users/chrissaechao/IdeaProjects/lender-search/app/.eslint-fixture.ts` with this exact content (the leading dot is intentional — keeps it out of typecheck `include` while still being linted):

    ```typescript
    /**
     * SMOKE FIXTURE — intentionally violates eslint rules to prove they fire.
     *
     * Phase 1 has no real `app/` code. This file lives at `app/.eslint-fixture.ts`
     * (leading dot keeps it out of tsc include) and contains forbidden patterns
     * that the eslint.config.mjs rules reject.
     *
     * Expected `pnpm lint` output:
     *   - error  Read env vars from `lib/env.ts` ...  no-restricted-properties
     *   - error  service_role / admin DB clients ...  no-restricted-imports
     *
     * The CI step `pnpm lint` runs with --max-warnings=0 against the project
     * (excluding this fixture). To exercise the rule explicitly, Plan 08 Task 3
     * adds a CI step that runs `pnpm lint -- app/.eslint-fixture.ts` and
     * INVERTS the exit code (expecting failure).
     *
     * Phase 6 should DELETE this file when real app/ code lands.
     */

    // VIOLATION 1: process.env access from app path
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    const dbUrl = process.env.DATABASE_URL;

    // VIOLATION 2: SUPABASE_SERVICE_ROLE_KEY read from app path
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY;

    // VIOLATION 3: imports a forbidden pattern.
    // (Cannot actually import — module doesn't exist; left as a comment so the
    //  pattern is grep-discoverable for future verification.)
    // import { adminDb } from './admin-db';
    // import { serviceRoleClient } from '../service-role';

    export {};
    ```

    Verify the fixture is rejected by ESLint:
    ```bash
    cd /Users/chrissaechao/IdeaProjects/lender-search
    # This MUST exit non-zero (errors found in fixture):
    pnpm exec eslint app/.eslint-fixture.ts ; echo "exit code: $?"
    ```

    Expected output: exit code != 0, with at least 2 `error` lines mentioning `no-restricted-properties` (one for the DATABASE_URL read, one for SUPABASE_SERVICE_ROLE_KEY).

    Then verify the rest of the project lints clean (the fixture is excluded from default lint via the file pattern OR by adding it to ignores during normal lint):
    ```bash
    pnpm lint  # should EXCLUDE the fixture (we want regular lint to pass)
    ```

    Hmm — by default `pnpm lint` is `eslint .` which includes the fixture and would fail. We need TWO lint invocations:
    1. `pnpm lint` — excludes the fixture; must pass on the rest of the codebase.
    2. A separate CI-only step that explicitly lints the fixture and asserts it FAILS.

    Update `package.json` `lint` script to ignore the fixture by default. Edit `/Users/chrissaechao/IdeaProjects/lender-search/package.json` and change the `lint` line to:
    ```json
        "lint": "eslint . --ignore-pattern 'app/.eslint-fixture.ts'",
        "lint:fixture": "eslint app/.eslint-fixture.ts && exit 1 || exit 0",
    ```

    The `lint:fixture` script exits 0 ONLY if eslint exits non-zero (rule fired) — it inverts the exit code. CI runs both `pnpm lint` AND `pnpm lint:fixture`.

    Verify:
    ```bash
    pnpm lint           # exits 0 (rest of project clean)
    pnpm lint:fixture   # exits 0 (because eslint exited non-zero on the violation)
    ```
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/IdeaProjects/lender-search && test -f eslint.config.mjs && test -f app/.eslint-fixture.ts && grep -q "no-restricted-imports" eslint.config.mjs && grep -q "SUPABASE_SERVICE_ROLE_KEY" app/.eslint-fixture.ts && grep -q "lint:fixture" package.json && pnpm lint && pnpm lint:fixture && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `eslint.config.mjs` exists at repo root and uses flat-config format (`import tseslint from 'typescript-eslint'`)
    - Config contains `no-restricted-properties` rule blocking `process.env` for `app/**`, `pages/**`, `src/**`
    - Config contains `no-restricted-imports` rule blocking `**/service-role*` and `**/admin-db*` patterns
    - Config ignores legacy files: `src/App.js`, `src/App.test.js`, `src/index.js`, `src/setupTests.js`, `src/reportWebVitals.js`, `public/**`, `node_modules/**`, etc.
    - File `app/.eslint-fixture.ts` exists with at least 2 violations (DATABASE_URL and SUPABASE_SERVICE_ROLE_KEY reads via process.env)
    - `package.json` scripts: `lint` excludes the fixture via `--ignore-pattern`; `lint:fixture` runs eslint on the fixture with INVERTED exit code (exit 0 only when violation found)
    - `pnpm lint` exits 0 (rest of codebase clean)
    - `pnpm lint:fixture` exits 0 (because the fixture fails lint as expected)
  </acceptance_criteria>
  <done>
    ESLint rule is wired and verifiable today. Phase 6's first `app/` PR will be gated by this rule automatically. The fixture is the smoke test that the rule fires; CI runs both forms.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .github/workflows/ci.yml (Postgres 16 service container + migrate + lint + typecheck + test:rls)</name>
  <files>
    - .github/workflows/ci.yml
  </files>
  <read_first>
    - /Users/chrissaechao/IdeaProjects/lender-search/.planning/phases/01-tenant-isolation-foundation/01-RESEARCH.md §"Pattern 7: Postgres 16 Local Dev + CI" (verbatim CI workflow YAML — adapt to align with Plan 06 globalSetup which migrates + grants)
    - /Users/chrissaechao/IdeaProjects/lender-search/.planning/phases/01-tenant-isolation-foundation/01-CONTEXT.md §D-09 (CI shape) AND §D-13 (GitHub Actions secrets)
    - /Users/chrissaechao/IdeaProjects/lender-search/scripts/init-db.sh (Plan 01 — local provisioning; CI replicates inline)
    - /Users/chrissaechao/IdeaProjects/lender-search/tests/rls/global-setup.ts (Plan 06 — already runs migrate + grant)
    - /Users/chrissaechao/IdeaProjects/lender-search/package.json (scripts: db:migrate, lint, lint:fixture, typecheck, test:rls)
  </read_first>
  <action>
    Create directory `/Users/chrissaechao/IdeaProjects/lender-search/.github/workflows/` if missing.

    Create `/Users/chrissaechao/IdeaProjects/lender-search/.github/workflows/ci.yml` with this exact content:

    ```yaml
    name: CI

    on:
      pull_request:
        branches: [main]
      push:
        branches: [main]

    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    jobs:
      verify:
        name: Lint, typecheck, RLS pen tests
        runs-on: ubuntu-latest
        timeout-minutes: 10

        services:
          postgres:
            image: postgres:16-alpine
            env:
              POSTGRES_USER: postgres
              POSTGRES_PASSWORD: postgres
              POSTGRES_DB: lender_search_test
            ports:
              - 5432:5432
            options: >-
              --health-cmd "pg_isready -U postgres"
              --health-interval 5s
              --health-timeout 3s
              --health-retries 10

        env:
          # Pen tests connect as app_user (NOBYPASSRLS NOSUPERUSER per CONTEXT D-07).
          DATABASE_URL: postgresql://app_user:app_user_password@localhost:5432/lender_search_test
          # globalSetup uses MIGRATION_DATABASE_URL to apply migrations as the postgres
          # superuser (per Plan 06 / Plan 05 — only the owner can ALTER TABLE FORCE).
          MIGRATION_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lender_search_test
          # Pen-test JWT signing secret. CI value is fixture-only; production uses
          # Vercel encrypted env (Phase 6).
          RLS_TEST_JWT_SECRET: ci-fixture-jwt-secret-min-16-chars

        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Install pnpm
            uses: pnpm/action-setup@v4
            with:
              version: 9

          - name: Setup Node.js
            uses: actions/setup-node@v4
            with:
              node-version: 20
              cache: pnpm

          - name: Install dependencies
            run: pnpm install --frozen-lockfile

          - name: Provision app_user role (NOBYPASSRLS NOSUPERUSER)
            env:
              PGPASSWORD: postgres
            run: |
              psql -h localhost -U postgres -d lender_search_test \
                -c "CREATE ROLE app_user LOGIN PASSWORD 'app_user_password' NOBYPASSRLS NOSUPERUSER;
                    GRANT CONNECT ON DATABASE lender_search_test TO app_user;
                    GRANT USAGE ON SCHEMA public TO app_user;"

          - name: Typecheck
            run: pnpm typecheck

          - name: Lint (excludes intentional ESLint fixture)
            run: pnpm lint

          - name: Lint fixture (proves no-restricted-* rules fire on violations)
            run: pnpm lint:fixture

          - name: Apply migrations + grant DML to app_user (replicates Plan 06 globalSetup)
            env:
              DATABASE_URL: postgresql://postgres:postgres@localhost:5432/lender_search_test
            run: |
              pnpm drizzle-kit migrate
              PGPASSWORD=postgres psql -h localhost -U postgres -d lender_search_test \
                -c "GRANT SELECT, INSERT, UPDATE, DELETE ON TABLE tenant, _rls_canary TO app_user;
                    GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO app_user;"

          - name: Verify FORCE ROW LEVEL SECURITY is on (TNT-01 gate)
            env:
              PGPASSWORD: postgres
            run: |
              FORCED=$(psql -h localhost -U postgres -d lender_search_test -t -A -F'|' \
                -c "SELECT relname || '|' || relforcerowsecurity FROM pg_class WHERE relname IN ('tenant', '_rls_canary') ORDER BY relname")
              echo "$FORCED"
              echo "$FORCED" | grep -q "^_rls_canary|t$" || { echo "ERROR: _rls_canary FORCE RLS not on"; exit 1; }
              echo "$FORCED" | grep -q "^tenant|t$" || { echo "ERROR: tenant FORCE RLS not on"; exit 1; }

          - name: Run RLS pen-test suite (TNT-04 merge gate)
            run: pnpm test:rls
    ```

    Verify the YAML parses correctly:
    ```bash
    cd /Users/chrissaechao/IdeaProjects/lender-search
    # Lightweight YAML sanity (most CI lint tools require GitHub):
    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "YAML OK"
    ```
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/IdeaProjects/lender-search && test -f .github/workflows/ci.yml && python3 -c "import yaml; data = yaml.safe_load(open('.github/workflows/ci.yml')); assert 'jobs' in data; assert 'verify' in data['jobs']; assert any('test:rls' in str(s) for s in data['jobs']['verify']['steps']); print('YAML OK')" && grep -q "postgres:16-alpine" .github/workflows/ci.yml && grep -q "pnpm test:rls" .github/workflows/ci.yml && grep -q "pnpm lint:fixture" .github/workflows/ci.yml && grep -q "FORCE ROW LEVEL SECURITY is on" .github/workflows/ci.yml && grep -q "cancel-in-progress: true" .github/workflows/ci.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/ci.yml` exists and is valid YAML (parses with python yaml.safe_load)
    - Workflow triggers on `pull_request` AND `push` to `main`
    - Workflow has `concurrency.cancel-in-progress: true`
    - Workflow uses `postgres:16-alpine` service container with healthcheck
    - Workflow env block sets `DATABASE_URL` (app_user) AND `MIGRATION_DATABASE_URL` (postgres) AND `RLS_TEST_JWT_SECRET`
    - Steps in order: checkout → pnpm setup → node setup → install → provision app_user → typecheck → lint → lint:fixture → migrate + grant → verify FORCE → test:rls
    - The "Verify FORCE ROW LEVEL SECURITY is on" step queries `pg_class.relforcerowsecurity` AND fails if either table is not forced
    - Workflow timeout is 10 minutes (sane upper bound)
  </acceptance_criteria>
  <done>
    CI workflow is wired. Every PR runs typecheck, lint (both forms), migrate + grant, FORCE RLS verification, and the full pen-test suite. Any failure blocks merge.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update README.md with the PR gate documentation and run final end-to-end smoke from a fresh state</name>
  <files>
    - README.md
  </files>
  <read_first>
    - /Users/chrissaechao/IdeaProjects/lender-search/README.md (current — extend with PR-gate section)
    - /Users/chrissaechao/IdeaProjects/lender-search/package.json (final scripts list)
    - /Users/chrissaechao/IdeaProjects/lender-search/.github/workflows/ci.yml (the contract README documents)
  </read_first>
  <action>
    Read the current `/Users/chrissaechao/IdeaProjects/lender-search/README.md` and APPEND a new section at the bottom (do not replace existing content):

    ```markdown

    ## PR Gate (Phase 1+)

    Every pull request runs the following checks. Any failure blocks merge.

    | Check | Command | What it asserts |
    |-------|---------|-----------------|
    | Typecheck | `pnpm typecheck` | TypeScript compiles strict |
    | Lint | `pnpm lint` | App code does not access `process.env` directly; no service-role import patterns |
    | Lint fixture | `pnpm lint:fixture` | The intentional ESLint fixture violates rules and is rejected (proves rules fire) |
    | Migrate | `pnpm drizzle-kit migrate` | Schema applies cleanly from a fresh DB |
    | FORCE RLS gate | psql introspection | `pg_class.relforcerowsecurity = true` for both `tenant` and `_rls_canary` |
    | RLS pen tests | `pnpm test:rls` | All cross-tenant attacks return zero rows or are rejected |

    Run all of these locally before pushing:

    ```bash
    pnpm typecheck && pnpm lint && pnpm lint:fixture && pnpm test:rls
    ```

    See `.github/workflows/ci.yml` for the full CI definition.

    ### Why these gates exist

    Tenant isolation is the antitrust safety net — a single cross-tenant leak is existentially expensive in this domain (per `.planning/research/PITFALLS.md` § 3.1, citing the October 2025 Smith v. Optimal Blue class action). The pen-test suite turns "forgot a `WHERE` clause" into "no rows returned" instead of a data-exfiltration incident.

    Any RLS pen-test regression is treated with the same severity as a security incident.

    ## Phase 1 Summary (Tenant Isolation Foundation)

    Phase 1 ships:
    - Postgres 16 schema with `FORCE ROW LEVEL SECURITY` on every tenant-scoped table
    - Self-filtering RLS policies on `tenant` + `_rls_canary` using `current_setting('app.tenant_id', true)::uuid`
    - `setTenantContext(tx, tenantId)` primitive at `lib/tenant/context.ts` (Phase 6 wraps in request middleware)
    - Vitest pen-test harness at `tests/rls/` covering the D-03 matrix (cross-tenant SELECT/write, JWT tampering, service-role boundary, GUC reset, index scan)
    - t3-env runtime validation at `lib/env.ts` (boot fails closed on missing/malformed env vars)
    - GitHub Actions CI gating PRs on `pnpm test:rls` + `pnpm lint` + `pnpm typecheck`

    Out of scope (deferred to later phases):
    - Real domain tables (program, agency_rule, license, evaluation_event) → Phase 2+
    - Supabase Auth + JWT verification → Phase 6
    - `withTenantContext` request wrapper → Phase 6
    - Inngest worker pool, Sentry, Axiom → Phase 6
    ```

    Now run the final end-to-end smoke from a fresh state. This is the closing verification that Phase 1 is structurally complete:

    ```bash
    cd /Users/chrissaechao/IdeaProjects/lender-search

    # 1. Reset the docker volume so we start from absolutely empty.
    pnpm db:reset
    until docker compose exec postgres pg_isready -U postgres > /dev/null 2>&1; do sleep 1; done

    # 2. Apply migrations as postgres superuser.
    DATABASE_URL="postgresql://postgres:postgres@localhost:5432/lender_search_dev" pnpm drizzle-kit migrate

    # 3. Grant DML to app_user (CI's globalSetup does this; replicate locally for parity).
    docker compose exec -T postgres psql -U postgres -d lender_search_dev \
      -c "GRANT SELECT, INSERT, UPDATE, DELETE ON TABLE tenant, _rls_canary TO app_user;
          GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO app_user;"

    # 4. Verify FORCE RLS is on.
    docker compose exec -T postgres psql -U postgres -d lender_search_dev \
      -c "SELECT relname, relrowsecurity, relforcerowsecurity FROM pg_class WHERE relname IN ('tenant', '_rls_canary')"

    # 5. Run typecheck + lint + pen tests.
    pnpm typecheck && pnpm lint && pnpm lint:fixture && pnpm test:rls
    ```

    Expected: all steps exit 0; the FORCE check shows `t | t` for both tables; the pen-test suite shows ≥20 cases passing; total runtime <60 seconds for the full sequence.

    If any step fails:
    - Migration error → check Plan 04 / Plan 05 SUMMARYs
    - typecheck error → check imports across plans
    - lint error → check ESLint config exclusions; legacy `src/` files should be in ignores
    - pen-test error → consult Plan 06 SUMMARY (harness) or Plan 07 SUMMARY (assertions)
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/IdeaProjects/lender-search && grep -q "## PR Gate" README.md && grep -q "pnpm test:rls" README.md && grep -q "## Phase 1 Summary" README.md && pnpm typecheck && pnpm lint && pnpm lint:fixture && pnpm test:rls 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains a `## PR Gate` section AND a `## Phase 1 Summary` section
    - The PR Gate section includes a table mapping each check to its command and asserts
    - The PR Gate section includes the one-liner `pnpm typecheck && pnpm lint && pnpm lint:fixture && pnpm test:rls`
    - End-to-end smoke from a fresh state succeeds: `pnpm db:reset` → migrate → grant → FORCE check → typecheck + lint + lint:fixture + test:rls all exit 0
    - Total smoke runtime <60 seconds
    - Pen-test suite shows ≥20 individual cases passing
  </acceptance_criteria>
  <done>
    Phase 1 is structurally complete. README documents the PR gate. Local smoke from a fresh state runs green in under a minute.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 [CHECKPOINT]: Push branch and confirm CI runs green on GitHub</name>
  <files>
    - (no files modified — human verification of CI workflow against GitHub Actions)
  </files>
  <read_first>
    - /Users/chrissaechao/IdeaProjects/lender-search/.github/workflows/ci.yml (Task 2 output — the workflow being verified)
    - /Users/chrissaechao/IdeaProjects/lender-search/README.md (Task 3 — PR gate documentation)
  </read_first>
  <what-built>
    All Phase 1 artifacts complete:
    - Workspace + tooling baseline (Plan 01)
    - t3-env validation (Plan 02)
    - Drizzle client + tenant context primitive (Plan 03)
    - Schema + initial migration (Plan 04)
    - FORCE RLS migration + applied migrations (Plan 05)
    - Pen-test harness (Plan 06)
    - 6 cross-tenant pen tests + env-boot test (Plan 07)
    - ESLint rule + ESLint fixture + GitHub Actions CI workflow + README PR gate (Plan 08)

    Local smoke (`pnpm db:reset && drizzle-kit migrate && pnpm test:rls`) runs green.
    The branch needs pushing so the GitHub Actions workflow runs against a real CI environment.
  </what-built>
  <action>
    HUMAN VERIFICATION required. Claude has prepared all Phase 1 artifacts and the local smoke is green; this checkpoint pauses for the user to push the branch, watch the GitHub Actions CI workflow, and confirm the merge gate operates correctly. See `<how-to-verify>` for the exact steps.

    Claude does NOT push the branch automatically — pushing is a deliberate user action that triggers CI billing and remote state. The user types "approved" once CI is green AND a deliberate-regression demonstration confirms CI catches failures.
  </action>
  <how-to-verify>
    1. Commit the Phase 1 changes (the orchestrator may have done this already; if not, commit using the project's standard message format with `Co-Authored-By` line).
    2. Push the branch to GitHub:
       ```bash
       git push -u origin <current-branch-name>
       ```
    3. Open the GitHub Actions tab for the repository:
       - URL: `https://github.com/<owner>/lender-search/actions`
       - The "CI" workflow triggered by the push should be running.
    4. Wait for the workflow to complete (expected runtime ~3-5 minutes including dep install).
    5. Verify all steps are green:
       - Typecheck
       - Lint (excludes fixture)
       - Lint fixture (exits 0 because fixture violations are detected)
       - Apply migrations + grant DML to app_user
       - Verify FORCE ROW LEVEL SECURITY is on
       - Run RLS pen-test suite
    6. Open a draft PR against `main` and confirm:
       - The "CI / verify" check appears in the PR's checks section
       - The PR has a status check that prevents merge if CI is failing (configure branch protection rules separately if not already in place)
    7. INTENTIONALLY break a pen test to confirm CI catches it. For example:
       - Edit `tests/rls/cross-tenant-select.test.ts` and change the first `expect(rows).toHaveLength(0)` to `expect(rows).toHaveLength(1)` (a wrong assertion).
       - Push that commit.
       - Confirm the CI workflow turns red AND the PR check is failing.
       - Revert the change with `git revert HEAD` and push again — confirm CI turns green.

    Type "approved" when:
    - The CI workflow is green on the latest commit of the branch
    - The intentional-break-and-revert demonstration confirmed CI catches regressions
    - You're satisfied that Phase 1 is structurally complete

    Type a description of any issues if something didn't work as expected.
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — human verifies CI run on GitHub. No automated check at this step."</automated>
  </verify>
  <done>
    User has typed "approved" after watching the GitHub Actions workflow run green on the pushed branch AND verifying the deliberate-regression demonstration caught the failure. Phase 1 is structurally complete; the antitrust safety net is operational; the pen-test suite gates merge.
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → service container postgres | CI applies migrations as superuser; pen tests run as app_user |
| application code → process.env | ESLint rule blocks direct access from app paths (Plan 08); `lib/env.ts` is the only sanctioned reader (Plan 02) |
| application code → service-role connection | ESLint import-block rule prohibits `service-role*`/`admin-db*` patterns from app paths |

## STRIDE Threat Register (this plan only)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege (BYPASSRLS in app path) | eslint.config.mjs no-restricted-imports rule | mitigate (full layer over Plan 06's runtime sanity check) | ESLint rule blocks `service-role*` / `admin-db*` import patterns from `app/**`, `pages/**`, `src/**`. The fixture `app/.eslint-fixture.ts` exercises the rule today; CI runs `pnpm lint:fixture` to assert violations are caught. Phase 6's first `app/` PR is automatically gated. |
| T-01-07 | Information Disclosure (plaintext secrets) | eslint.config.mjs no-restricted-properties rule | mitigate (full layer over Plan 02 t3-env) | ESLint rule blocks `process.env` reads from app paths; only `lib/env.ts` (the sanctioned reader) is allowed. CI runs `pnpm lint` (excludes fixture) AND `pnpm lint:fixture` (asserts the rule fires on violations). |
</threat_model>

<verification>
1. `eslint.config.mjs` flat config exists with both rules; `app/.eslint-fixture.ts` exists and is rejected by `pnpm lint:fixture`
2. `.github/workflows/ci.yml` is valid YAML with the full step ordering: install → provision app_user → typecheck → lint → lint:fixture → migrate + grant → FORCE check → test:rls
3. README documents the PR gate with the one-line check command
4. End-to-end local smoke from a fresh state runs green in <60 seconds
5. The checkpoint pauses for the user to push the branch and verify CI catches an intentional regression
</verification>

<success_criteria>
- ESLint rule is wired and the smoke fixture exercises it (CI runs both `lint` and `lint:fixture` with inverted exit code)
- GitHub Actions CI replicates the local flow: install → provision app_user → migrate as postgres → grant DML → FORCE RLS verification → pen tests as app_user
- README PR-gate section documents the merge gate
- Local smoke from a fresh state (`pnpm db:reset && pnpm drizzle-kit migrate && pnpm test:rls`) runs green in <60 seconds
- The user has pushed the branch, watched CI run green, and verified that a deliberate regression is caught
- Phase 1's antitrust safety net is operational: cross-tenant attacks are structurally rejected and CI gates merge
</success_criteria>

<output>
After completion, create `.planning/phases/01-tenant-isolation-foundation/01-08-SUMMARY.md` documenting:
- Confirmation that the GitHub Actions workflow ran green on the first push
- The intentional-break demonstration result (which test caught the deliberate regression?)
- Total CI runtime (the workflow's first run; subsequent runs are typically faster due to pnpm cache)
- Whether branch protection rules were configured to require the CI check (or whether that's a separate manual step the user took outside the GSD flow)
- Any divergence between local smoke and CI smoke (e.g., did CI surface a Postgres-version-specific behavior?)
- Confirmation that Phase 1's must_haves from ROADMAP §"Success Criteria" are all green:
  1. ✓ Cross-tenant query as A → 0 rows when targeting B's data (verified by tests/rls/ in CI)
  2. ✓ Postgres 16 + Drizzle 0.45 + prepare:false (Phase 1 contract; Supabase project ships Phase 6)
  3. ✓ JWT tampering + service-role + cross-tenant write/read tests fail closed
  4. ✓ tenant_id indexed on every tenant-scoped table; RLS uses current_setting('app.tenant_id')::uuid
  5. ✓ No plaintext secrets in source; t3-env validates required keys at boot
</output>
